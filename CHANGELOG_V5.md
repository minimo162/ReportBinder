# ReportBinder V5 実装履歴

## Excelの半角数字シート既定化とページ構成UX改善（2026-08-17）

- 新規Excel原稿は、シート名がstrictな半角数字だけ（`^[0-9]+$`）の表示シートをPDF化する`numeric-only`を既定として明示保存する。既存の明示`all-visible`/`numeric-only`設定と、旧データで選択項目が欠けている場合の互換動作は上書きしない。設定は自動処理・旧API・バックグラウンドジョブにも引き継ぎ、ジョブ開始時の対象指定を固定する。
- 数字のみモードでは非該当・非表示シートをPDF化せず、既存のページID・ページ名・番号・使用範囲・その他の設定を保持したまま、古いcontent PDFを外し、`not-rendered`・未振り分けとして明示する。数字シートが0件の場合は空PDFやページ構成を保存せず、シート名の修正を促す。
- ページ構成画面に「半角数字シートをまとめる」を追加した。指定出力先のExcelスロットへ安全な数字順で配置し、スロットのない場合は末尾へ追加する。Excel以外のページはアンカーとして配置と順番を保持し、非該当Excelは未振り分けへ移す。既存のreorder APIの競合検出・履歴・Undoを利用し、変更がない場合はリクエストを送らない。
- RuntimeVersion `2026.08.17.3` へ更新した。

- ページ構成の初期表示と新規ページ生成を、各レーンの半角数字シート数値順（桁数→文字列順）へ変更した。既存の手動順はPDF再作成で上書きせず、明示的な「半角数字順に並べ直す」で未振り分け・本体・補足を再整理できる。Excel以外と非対象Excelはアンカーとして保持し、収集操作とは別の機能として扱う。
- ページ構成ボードを画面全幅に広げ、ブラウザー全体の縦スクロールを基本に変更した。未振り分け・本体・補足レーン内の固定高さ・縦スクロールをなくし、カード幅と空間を再配分した。

## ページ構成を全体俯瞰できる整理画面へ改修（2026-08-17）

- ページ構成の既定表示を、1つの置き場所だけを切り替えるタブ表示から、未振り分けと各出力先を同時に見渡せるボードへ変更した。大量ページでは「全体／未振り分け／各出力先」タブで1か所へ絞り込める。
- 「表示中をすべて選ぶ」がCSSで非表示の別出力先まで選ぶ不具合を修正した。タブを切り替えても選択は保持し、現在位置ではなく実際の選択ページの置き場所を見て移動先ボタンを無効化する。
- サムネイル操作を、左上のチェック＝選択、右上のハンドル＝移動、画像＝プレビューに分離した。カード全体の5pxドラッグを廃止し、タッチ操作でも縦スクロールと並べ替えが衝突しないようにした。
- 検索と「変更分のみ」を固定操作バーへ移し、検索結果が0件のときは同じ場所から1クリックで解除できるようにした。PDF未作成・ページ0件の状態にも次の操作へのボタンを追加した。
- 並べ替えの「保存中／保存済み／保存できません」を操作バーに常設した。V2並べ替え経路が `baseLayout` を捨てて競合検出を無効にしていた問題も修正し、古いタブから新しいページ構成を上書きできないようにした。
- 盤面の再描画判定へ出力先名、原稿名、警告、変更情報、変換PDFの版を含めた。配置変更だけで同じ出力先の全サムネイルキャッシュが失効する問題も解消した。
- 3列CSSの古い文字列が残っているだけで合格していた回帰検査を、最後に有効な宣言を判定する検査へ変更した。後段に `display:block` を足すと実際に落ちる否定テストも通している。
- RuntimeVersion `2026.08.17.1` へ更新した。

## 版番号の据え置きで配布物が届かなくなる欠陥を塞ぐ（2026-08-15）

- 版番号が `2026.08.10.4` のまま2つの変更（#59 #60）が入り、**共有フォルダー用フォルダーを作り直しても、既にその版を持つPCへは届かない**状態になっていた。`launch.ps1` のローカル版が最新かの判定は `installed.json` / `server.ps1` / `web\app.js` の**存在**だけを見ており、内容も日付も比べない。実機で確認したところ、配布物の `web\app.js` が 437,984B、同じ版番号のローカルキャッシュが 433,811B と食い違っていた。
- 上げ忘れを止めるはずだった `selfcheck.py` の検査は `if runtime_version != '2026.08.10.4'` という**リテラル固定**で、版番号を上げたときだけ落ちる。上げ忘れは素通りするため、#59 と #60 を実際に通していた。`app/` の配布対象ファイルが変わったのに版番号が据え置きなら落ちる検査に置き換えた。除外の判断は `package-release.ps1` の除外一覧をその場で読んで行い、一覧を二重に持たない。
- fast-gate は `actions/checkout` の既定（`fetch-depth: 1`）で履歴が無く、基準との比較ができなかった。fast-gate だけ全履歴を取得するようにした。
- 門は「版番号が変わっていれば通す」ではなく「基準より真に新しく、かつ基準時点の CHANGELOG に未出であること」を要求する。等値だけを見ると、同じ日に開いた2つのPRが同じ連番を選んだ場合も、過去の版へ巻き戻した場合も素通りし、いずれも #59 #60 と同じ結末（後から入った変更が永久に届かない）になる。
- 差分は三点（`base...HEAD`）と二点（`base HEAD`）の積で取る。squash マージ運用では、先行PRがマージされた時点で後続PRの merge-base が分岐元まで戻り、三点だけだと後続PRが自分の触っていないファイルで落ちる。
- `pack-lifecycle-selfcheck.ps1`、`custom-pack-workflow-selfcheck.ps1`、`tests\diff-regression.mjs` が除外一覧から漏れて配布物へ混入していた。開発専用なので除外し、ZIP側の検査にも足した。門から見ると配布対象と判定されるため、利用者の挙動が変わらない変更にまで版番号の更新を強いていた。`tests` は1本ずつ並べる書き方をやめてディレクトリごと除外した。1本ずつだと追加のたびに書き足す必要があり、実際に `diff-regression.mjs` が漏れていた。
- RuntimeVersion `2026.08.15.1` へ更新した。

## 未着手だった3件の対応（2026-08-10）

- 一式の「複製」は設定だけをコピーし、登録した原稿もページの並びも引き継がない（仕様）。しかしボタンは「複製」としか書かず、成功の通知も触れていなかったため、先月と同じ体裁で今月分を作ろうとした人が、登録40件とページ180件の振り分けをやり直すことになっていた。ボタンを「設定だけ複製」に改め、押す前に何が引き継がれないかを示し、毎月同じ資料をまとめるなら同じ一式を使い続け、名前の変わった原稿は付け替えるという正しい進め方へ導く。
- 原稿の付け替えが1件ずつしかできず、ファイル名が毎月変わる運用では30件で90〜120クリックになっていた。見つからない原稿が2件以上あるときに、対応表をまとめて表示して1回のボタンで確定できるようにした。中身が同じファイルは初期選択の根拠にするが、自動では確定しない。誤った対応付けは提出物の中身そのものを誤らせるため、必ず目で見てから決める形にしている。
- 「ひな形管理」が一式の「新規作成」の隣に同じ大きさで置かれ、押すと必要原稿リストや出力条件のフォームが開き、確定ボタンは「作成」だった。何が作られるのか分からないまま押させていたので、アプリメニュー側へ移し、名前も用途が分かるものにした。確定ボタンも「ひな形を作成」「このひな形を保存」に分けた。
- RuntimeVersion `2026.08.10.4` へ更新した。


## 不利なペルソナを前提にしたUI/UX修正（2026-08-10）

「62歳・部長職・ITに不慣れ・老眼・非常に多忙・月1〜2回しか触らない」を、この人が使えなければ失敗とみなす前提として4観点で調査し、14件に対応した。

- 起動のたびに**最も古い一式**が選ばれていた。選択が `sessionStorage` にしか無く、`.cmd` からの起動は毎回新しいタブなので空になり、既定が作成順の先頭だったため。1か月ぶりに開いた人が先月以前の一式に今月の原稿を登録し、提出用PDFを開くまで気づかない。`localStorage` に最後に使った一式を保存し、復帰先を最終更新の新しいものにした。一覧も新しい順にし、各行に最終更新日を出す。
- 原稿の登録解除が、ページの振り分け・並び順・ページ名を消すのに、ダイアログは「作成済みの変換PDFと元の原稿ファイルは削除されません」としか言わず、逆の安心を与えていた。しかも破壊的操作で唯一 `Save-LayoutSnapshot` を呼んでおらず、戻す手段が一つも無かった。復元点を取り、失われるページ数を文言とボタンに出し、名前を変えただけなら付け替えを使うよう案内する。
- 差分モーダルが画面より高く、1366x768@125%で上下各47px（150%では98px）が画面外に出ていた。`place-items:center` のためスクロールでも取り戻せず、閉じるボタンと「次のページ」が押せなかった。
- 新しいページは既定で全部「未振り分け」に入るのに、「表示分をすべて選択」が閉じた折りたたみの中にしかなく、サムネイル表示には全選択チェックも無かった。180ページを1枚ずつクリックさせていた。移動先バーに「表示中の◯ページをすべて選ぶ」を常時出す。
- 失敗したときの出口が無かった。ほぼ全操作の失敗が落ちる `runBusy` の catch にボタンが1つも無く、変換エラーの通知にも無かった（提出用PDF側にはあった）。「もう一度試す」と「動作環境を診断」を出す。起動失敗の文言も、正しい案内が既にあるのに使われていなかった。
- 文字サイズ13px以下が41宣言残っていた。`style.css` 自身が「13pxの日本語は対象利用者には小さすぎる、14pxが下限」と書いているのに守れていなかった。すべて14px以上にし、以後13px以下を書けないよう検査する。
- `opacity:.55` により「移動先」ラベルが2.17:1、無効ボタンが1.96:1まで沈んでいた。`::placeholder` の定義が無くブラウザ既定の3.73:1で、絞り込み欄はラベルが `sr-only` のためそれが唯一の手がかりだった。透明度をやめ、無効状態の既定スタイルに委ねる。
- 24x24px未満のクリック領域が6件（全選択チェックボックス、折りたたみ見出し2件、スライダー2件）。見た目を変えずに当たり判定だけ広げた。
- 色だけで意味を伝えていた3件。提出用PDFナビの警告が6x6の琥珀の丸だけで3状態を表しラベルも無かった。出力先チップは3つの警告状態で文字が変わらなかった。差分領域が13個以上だと修正のタグが消え、琥珀と青の枠色だけの判別になっていた（加齢による青と黄の識別低下と正面衝突する）。
- 起動時の着地が毎回ステップ1で、再訪者向けの「次にやること」に一度もたどり着けなかった。新しいタブは「ホーム」に着地させる。完了済みの一式でも「次に、使うページと順番を確認します。」が出続けていたのも直した。
- 待ち時間が伝わらなかった。`document.title` を一度も更新しておらず、会議中にタブを見ても進捗が分からない。残り時間も出ない（`startedAt` を画面が読んでいなかった）。数十秒かかる「更新確認」の案内が4秒で消えていた。`.indeterminate` をJSが6回付けるのにCSS定義が0件で、「3%で止まったバー」に見えていた。
- 5分ごとの自動更新で、入力途中の出力ファイル名が警告なく元の値に戻っていた。触っている最中と未保存の変更があるときは上書きせず、保存前であることを画面に出す。赤いエラーパネルは閉じても自動更新のたびに復活し、入力中でもフォーカスとスクロールを奪っていた。同じ内容は開き直さず、移動は新しく開いたときだけにする。
- 拡大表示で崩れる3件。`.source-settings` はビューポート幅で折り返しを判定していたが実際の制約は列幅で、1920@125%では694pxが390pxの列を304pxはみ出していた。`.pack-progress-row` は1366@150%で `overflow:hidden` によりボタンが消えていた。履歴のファイル名列は90pxしかなかった。いずれもコンテナ相対にした。
- 同じものが「今回まとめる一式」「資料パック」「一式」と3通りに呼ばれていた。74箇所を「一式」に統一し、監査履歴・静止待ち・判定不能・PDF処理エンジンなどの内部用語も平易な日本語にした。
- RuntimeVersion `2026.08.10.3` へ更新した。

## Excel監視の欠陥修正と、原稿の付け替え（2026-08-10）

直前のコミット（`bd13b2a`）を敵対的にレビューさせ、そこで見つかった欠陥を先に潰してから、残っていた利用者側の課題に着手した。上位2件は正常動作を壊すもので、実機テストが踏んでいなかった。

- Excelの監視が、進捗ではなくExcelの起動時刻からの経過を測っていた。心拍を打つのは `Export-WorksheetToPdfSafe` だけで、2シート以上で既定になる `Export-WorkbookSheetsToPdfBatch` には無く、Excelはジョブ全体で1つを使い回すため、5分を超える正常な変換で健全なExcelが強制終了されていた。一括書き出しは1回の同期COM呼び出しでその内側では心拍を打てないので、心拍に「次までに許す時間」を持たせ、シート数ぶん猶予を伸ばしてから入る。進捗コールバックでも心拍を打つ。
- 組み込みパックの組版（利用者が最も通る経路）が `timedOut` を見ておらず、時間切れが「PDFBox組版に失敗しました。exit=-1」になっていた。件数で数える守りが、到達しない `Build-FinalPdfLegacy` を数に入れていたため抜けを検出できなかった。守りを呼び出しごとの検査に変え、死にコード40行を削除した。この削除により、死にコードを検査していた箇所が4件あることも判明したので現行経路へ向け直した（実質空振りだった）。
- 固まりやすい `Quit()` が監視の外に出ていたので、閉じ終えてから監視を止める順序にした。時間切れの説明を中央の `ConvertTo-UserRenderError` で行い、開く・閉じる・一括書き出しのどこで踏んでも同じ案内が出るようにした。
- 監視の枠は1組しかないのに無条件でリセットしていたため、2つ目のExcelを作ると現役のExcelが殺されうる状態だった。二重起動を例外にした。監視プロセスの作業ファイルが `%TEMP%` に無期限に溜まる問題も直した。
- 上限を900秒にした分、固まったときに中止が最大15分効かなくなっていた。外部コマンドの待ちを1秒ずつに刻み、実行中のジョブの中止要求を見るようにした。
- ローカルサーバーが終了した後、画面には英語の "Failed to fetch" しか出ず、復帰手段（`資料をPDFにまとめる.cmd` の再実行）がどこにも無かった。30分の無操作やノートPCのスリープで日常的に起きる。`api()` と PDF取得の両方で日本語の案内に変換し、ハートビートが3回続けて失敗したら画面でも知らせる。
- 原稿ファイルの名前を変える・移動すると、画面は「原稿更新あり／PDFを再作成してください」と表示するのに、その指示どおり押しても何も起きず、同時に「すべての変換PDFは最新です。」と出ていた。直す手段は登録解除→再登録しかなく、それはページの並び順と出力先の振り分けを全部消す。`missing` を「ファイルなし」として正しく出し、`workbookId` を据え置いたまま参照先だけ差し替える付け替えを追加した（`/api/v2/sources/relink`）。`workbookId` は `content-pdf\` `input-history\` `locks\` のディレクトリ名そのものなので変えない。中身が同じ（名前だけ変えた）場合は変換PDFの作り直しも不要になる。
- 「未登録の原稿はありません。」が、全部登録済み・対応形式が無い・資料パックが受け付けない形式、の3つの状況で同じ文言だった。どれも作業完了と読めるため言い分けた。未登録原稿の絞り込みは実装済みなのに入力欄がHTMLに無く到達不能だったので、入力欄を足した。
- `docs/API.md` が、現行UIが一度も呼んでいない6件のV2経路を正式仕様として区別なく列挙していた。実態を注記し、`PATCH /api/v2/items/{itemId}` が `baseLayout` を受け取らず競合検出を素通りすることを明記した。
- RuntimeVersion `2026.08.10.2` へ更新した。

## 多観点調査にもとづく欠陥修正（2026-08-10）

4観点（利用者体験・信頼性・保守性・棚卸し）の調査結果を突き合わせ、影響の大きい順に対応した。

- 外部コマンドの上限が全8箇所で既定の120秒のままだった。120秒は Word/PowerPoint の COM 変換に合わせた値で、java の実計算に流用できるものではない。分割はコード自身が2000ページのPDFを許容し、組版の出力先は共有フォルダーにもなる。さらに返り値の `timedOut` をどの呼び出し側も見ていなかったため、健全な大きいPDFに「破損していないか確認してください」と表示していた。用途別の上限（分割・組版15分、解析5分、java -version 30秒）を明示して渡し、時間切れは時間切れとして伝える。あわせて `java -version` の終了コードを見るようにした。起動できない java でもテキストが返れば「使える」と診断しており、共有フォルダー配布の初回展開という、診断画面が最も頼られる場面で誤報していた。
- `schema-v3-selfcheck.ps1` を変更ごとの門に戻した。外部依存ゼロの1.7秒で、`structure.json` のスキーマ移行という利用者データを不可逆に壊しうる唯一の種類を守る検査であり、手動実行に寄せるべきではなかった。あわせて重い受け入れスイートに週1の定期実行を足した。手動実行だけにした結果、`release.yml` も `workflow_dispatch` のみのため、自動で走る経路が一つも無くなっていた。
- 出力した提出用PDFの実体に到達する手段が無かった。別タブで開けるのは表示だけで、メールに添付する・共有フォルダーへ置くには実体が要る。出力先カードに保存先の全体を表示し、「保存先フォルダーを開く」でエクスプローラーに選択状態で開くようにした（`/api/v2/outputs/reveal`。出力フォルダーの外は開かない）。出力完了の通知もファイル名だけでなく保存先を出す。
- サーバー側に旧呼称「最終PDF」が23件残っていた。画面側は統一済みだったが、サーバーの日本語 `throw` は加工されずに画面へ出るため、提出用PDFを開いたまま再出力するといった日常の経路で、画面に無い言葉が利用者に見えていた。
- 書かれているのに一度も実行されていない回帰テストが3本あった（Word/PDFの原稿アダプタ、履歴の汎用化）。いずれも存在必須リストに名前があるだけだった。PDF と履歴は受け入れスイートに載せ、Word は実際の Word 変換を要するため Office のある機械でだけ実行し、無い場合は飛ばしたことを出力に残す。
- Excel の変換だけ時間制限が無く、固まると「PDF作成を中止」も効かなかった（Word/PowerPoint には120秒の制限と強制終了がある）。Excel は同一プロセス内の同期COM呼び出しで、PowerShell のタイマーやイベントでは救えない（COM呼び出しでパイプラインが塞がっている間ハンドラが実行されない）。外部の監視プロセスに見張らせ、1シートごとの心拍が5分止まったら、こちらが起動した Excel だけを終了させる。利用者が自分で開いている Excel を巻き添えにしないよう、ウィンドウハンドルから所有プロセスを特定する。
- 提出用PDFの控え（アーカイブ）の作成に失敗したことが握りつぶされ、画面にも記録にも残っていなかった。控えを作る経路には、出力の元になった原稿の版を後日の掃除から守る pin の作成が含まれており、pin の無い版は保持期間の猶予なしに削除の対象になる。pin の作成を控えから切り離し、控えに失敗しても保護だけは作るようにしたうえで、失敗を履歴と画面の両方に出す。
- RuntimeVersion `2026.08.10.1` へ更新した。

## UI/UX多観点レビューと実機確認による修正（2026-08-09）

情報設計・アクセシビリティ・ビジュアル・文言・インタラクションの5観点でレビューし、そのあと実際にアプリを起動して描画を見ながら再レビューした結果をまとめて反映した。

- ホーム画面が `VIEW_NAMES` にもナビにも無く、一度も表示されていなかった。「次にやること」の分岐、資料パック横断の進捗、最近の状況の実装がすべて到達不能だったため、サイドナビに「ホーム」を追加して復活させた。
- step-bar が資料パック作成と同時に `display:none` になり、1→2→3 の順序と進捗を示す唯一の要素が作業中だけ消えていた。常時表示にし、クリックで遷移できるようにしたうえで、`renderStepBar()` を `setActiveView()` からも呼んで現在地が追従するようにした。
- 進捗パネルが原稿画面の内側にあり、提出用PDFの出力中は進捗も中止ボタンも出ず、変換中に画面を切り替えると中止手段が消えていた。`<main>` 直下へ移した。
- 「移動先」と「選択した原稿を登録」が選択前は `display:none` で、画面の案内どおりに操作しようとしても押すべきものが存在しなかった。淡色・無効の状態で常時表示にした。
- 詳細表示のページ行はクリック処理が `.page-thumb-card` 限定で、案内どおりクリックしても選択できず、最も広いページ名入力を押すと編集に入っていた。行全体で選択できるようにした。
- 出力先パネルは表示中の1つ以外が `display:none` のため、ドラッグで他の置き場所へ移すことが原理的に不可能だった。出力先タブをドロップ先にし、ドラッグ中はタブ列を固定して重ねたタブを塗りつぶしで示す。移動はボタンと同じ経路を通るので取り消しも同じ。
- サムネイルの大きさスライダーが無効だった。`.thumbnail-board` のグリッドが幅をハードコードして CSS 変数を上書きしていた。
- `pattern` 属性が `v` フラグ下で不正（`-` の未エスケープ）で、HTML仕様により出力先IDの検証が丸ごと無視されていた。
- エラーが `display:none` のライブリージョンへ書き込まれ、支援技術に一切通知されていなかった。表示してから書き込み、`role="alert"` とフォーカス移動を追加した。詳細表示のページ行のフォーム部品には名前が無かった。
- コントラストを実測して是正した。アクセント文字 4.22→6.21、無効ボタン 2.0→4.0、差分の変更タグ 2.96→5.21、`--text-tertiary`、版カードの役割ボタン。
- 出力中の背景スキャンによる二重起動、処理中の資料パック切り替え、バルク移動中の他ボタン押下を防いだ。検索1文字での選択全消去、ドラッグ中の Esc が選択だけ解除して移動は確定していた問題も修正した。
- 成果物の呼称を「提出用PDF」に統一した（最終PDF／1つのPDF／提出できるPDF が混在していた）。フォルダー、アーカイブ、名前を変更、単位と日付書式も揃えた。エラー文から `install-thirdparty.cmd`、HTTPコード、ジョブ、content PDF、画像ハッシュ、JSON といった内部用語を除いた。
- `font-size:15px!important` が27件の指定を無効化していたため `:where()` にした。コントロール高の一律44pxを36pxへ戻し（`pointer:coarse` では44pxを維持）、1366x768 での横スクロールを解消した。
- 提出用PDFの出力後、`refresh()` と `loadFinalReadiness()` が続けて全体を描き直し、その間も進捗パネルが残ってから消えるため画面が点滅していた。状態の取得と描画を分け、1回だけ描くようにした。
- 出力完了の通知にあった「出力したPDFを確認」は `view:'final'` だけで、出力した本人はすでにその画面にいるため何も起きなかった。出力したPDFを直接開くようにし、出力先カード側の導線も「出力したPDFを開く」として枠付きのボタンに変えた。
- 資料をPDFにまとめる.cmd は `-LocalRuntime` なしで起動するため `%LOCALAPPDATA%` のコピーを再利用する。`runtime-version.json` を据え置くとこの入口からは古い画面が出続けるため、ランタイム版の日付と `app.js` のキャッシュバスターの日付が一致することを selfcheck で検査するようにした。
- 提出用PDF画面は、開くたびに readiness・出力履歴・レビューの3つを個別に読み込み、それぞれ完了時に別々へ描き直していた。`renderAll()` が `setActiveView()` を呼ぶため再描画のたびに再発火し、描いた数百ms後に画面が組み替わっていた。3つをまとめて読み終えてから1回だけ描くようにし、実際に画面へ入ったときだけ読み込むようにした。
- 出力先カードの3つのボタンが同じ重みで横並びで、出力後に何をすればよいか分からなかった。PDFができている間は「出力したPDFを開く」を主ボタンとして先頭に置き、再出力は右端の弱いボタンへ下げた。
- レビュー・承認をやめ、「確認の記録」に作り替えた。記録は %LOCALAPPDATA% 配下の利用者ごとのデータへ $env:USERNAME を承認者として書くだけで、他の利用者からは参照できない。提出・承認・差し戻しという相手のいる語をやめ、「確認済みにする」「確認を取り消す」の2操作にし、ローカルにしか残らないことを画面に明記した。保存はサーバー側の review API のまま。
- 確認の記録と作業の進み具合の連動を外した。原稿・変換PDF・ページ構成・出力がすべて済んでいても、確認していない限りホーム画面で「完了」にならず、要対応に数えられていた。この機能は最終画面の閉じた折りたたみの中にあるため、存在に気づかない利用者の完了件数は永久に0のままだった。完了は作業の進み具合だけで判定し、確認状態は資料パック行に別のバッジとして出す。
- ホーム画面などを開いた直後、ヘッダーが出力済みの資料パックを「提出用PDF 未出力」と誤表示していた。ヘッダー・ナビ・次にやることが参照する readiness を、提出用PDF画面に入ったときしか取得していなかった。どの画面からでも一度は取得する。
- スクロール中の点滅を解消した。固定ヘッダーとページ構成のコマンドバーに backdrop-filter が付いており、背景がほぼ不透明でブラーは見えないのに、スクロールのたびに背景の再ラスタライズが走っていた。DOMの変化は0件で、描画側だけの問題だった。ブラーはスクロールしないモーダルの暗幕にのみ残す。
- 起動直後の点滅を解消した。readiness を取得する前に最初の描画を行い、その直後にレイアウトを表示していたため、ヘッダーが「未出力」、提出用PDF画面が0ページの状態で一度見えてから正しい値に描き直されていた。最初の描画の前に取得する。
- 外部コマンドの起動に上限が無く、固まると呼び出し元ごと止まる欠陥を直した。`Invoke-NativeCapture` は `& $exe @args 2>&1 | ...` で待っており、待ち時間の上限が無かった。呼び出し先は9箇所すべて java で、そのうち PDF をラスタライズする `PdfPageAnalyzer` は Windows では `-Djava.awt.headless=true` を付けても AWT のツールキット(`sun.awt.windows.WToolkit`)を生成する。その初期化はウィンドウステーションが使えないと上限なしで待ち続けるため、CIのように対話デスクトップが不安定な環境では java が永久に返らず、PDF作成の操作ごと固まり得た。`ProcessStartInfo` で直接起動し、上限を過ぎたらプロセスツリーごと終了して `NATIVE_TIMEOUT` を返す。解析は元からクリティカルパス外なので、失敗として扱われても PDF 作成は成功のまま進む。
- 出力したPDFを別タブで開いたあと、アプリへの戻り方が分からなくなる懸念に対応した。開いた直後にアプリ側へ案内を残し(自動では消さない。利用者は別タブにいるため、戻ってきたときに読めないと意味がない)、開き先を名前付きタブにして使い回すようにした。従来は押すたびにタブが増え、どれが今見ているPDFなのか分からなくなっていた。
- CIの所要を詰めた。実測で重いのは Diff regression 204秒と PDF corpus 139秒で、どちらもピクセル差分の実計算だった(プロファイルで choosePixelRowMapping と pixelDifference が四割)。テストを削るとビジュアル差分の回帰網が薄くなるため、この2本を並列実行に変えた。あわせて、CIランナーのウイルス対策からワークスペースを除外する。同梱JREは毎回展開されるため、スイートで最初に起動するJVMが走査の費用を全部被っており、実測で1本目が約65秒、2本目以降が約1.6秒だった。
- 自動実行の配分を変えた。変更ごとに回すのは `fast-gate`（PowerShell構文、版番号とCHANGELOGの整合、`selfcheck.py --static-only`、JS構文）だけにし、約1分に収めた。重い受け入れスイートは `full-suite` ジョブへ分け、`workflow_dispatch` での手動実行と、`release.yml` が梱包前に必ず走らせる検証に寄せた。判断の根拠は、重いスイートがOfficeの入っていないランナーで動くため、利用者が毎日通るExcel / Wordの変換経路をそもそも実行できない（`source-adapter-selfcheck.ps1` が変換を差し替えている）こと。速い門が同梱JREを必要としないよう `selfcheck.py` に `--static-only` を追加し、飛ばした範囲は出力に明記する。配分が黙って崩れないよう、速い門への重い検査や依存導入の混入、`release.yml` が速い門で済ませる変更を selfcheck で検査する。あわせて、配布物スモークが変更ごとには走らなくなった分を梱包前が引き受けるよう、`release.yml` の `-SkipPackageSmoke` を外した。
- RuntimeVersion `2026.08.09.18` へ更新した。

## 多観点レビュー指摘への対応（2026-08-09）

多観点レビューで残っていた指摘を、深刻度順に対応した。

- 配布物に完全性検証を追加した。共有フォルダーの配布ツリーは各利用者のPCへコピーされてから実行されるのに照合が一切なく、判定材料は `runtime-version.json` の version 文字列だけだった。`app` 配下の全ファイルのSHA-256とサイズを `app\integrity-manifest.json` へ記録し、`%LOCALAPPDATA%` へ確定する前に照合する。不一致では共有コピーへフォールバックせず起動を中止する。共有全体に書き込める相手は検証自体を書き換えられるため回避できる点と、配布用共有を発行者以外読み取り専用にする前提をREADMEに明記した。
- シート名の変更と非表示でページ設定が失われないようにした。従来はシート名の一致だけで突合してページを削除し、削除時にスナップショットも取らず、`pageId` もシート名から導出されるため復元経路が存在しなかった。非表示シートは保持し、リネームは位置またはA1見出しの一致で `pageId` ごと引き継ぐ。真に削除する場合は確定前に復元ポイントを取る。
- ページ構成の保存に楽観ロックを入れた。並べ替えAPIは画面全体の並びを絶対値で送るのに版の検証が無く、タブを2枚開くと片方の変更が無言で巻き戻っていた。判定は配置そのものの指紋で行い、配置を変えない書き込み（更新スキャン等）では衝突させない。
- 原稿フォルダの変更に確認と復帰導線を入れた。確認なしでワークスペースごと切り替わり、旧パスがどこにも残らなかった。
- 提出用PDFの出力に進捗表示と中止を入れた。HTTPサーバーはリクエストを直列処理するため、出力を要求の中で完結させると進捗も中止も受け付けられない。変換PDFと同じく子プロセスへ出した。組み込みパックの一括出力は準トランザクションAPIを維持している。
- ページ構成画面をイベント委譲、検索のデバウンス、絞り込み時のDOM削減、ドラッグのrAF集約、画面外canvasの解放で軽くした。保存する並び順をDOMから読み取る設計のため、ビューポート単位のウィンドウイングは採用していない。
- RuntimeVersion `2026.08.08.36` へ更新した。

## 多観点レビューによる欠陥修正（2026-08-09）

セキュリティ・アクセシビリティ・信頼性・UX・性能の5観点で独立レビューを行い、実際に到達可能な破綻シナリオを示せた指摘を修正した。

- 更新検知でハッシュを取得できなかった回に、更新時刻とサイズだけを保存していた。次回スキャンが「メタデータ一致」で再ハッシュを省略するため、更新済みの原稿が「最新」に戻り、古い内容のまま提出用PDFが作られていた。ハッシュが取れない回はメタデータも据え置くようにした。
- ローカルAPIに Host / Origin の検証を追加した。ループバックのcookieはポートで分離されないため、127.0.0.1の別ポートで動くページがトークンを読んでAPIを実行できていた。
- 起動URLに含まれるトークンを、保存後にアドレスバーから取り除くようにした。ブラウザ履歴に資格情報が残らない。
- 変換PDFのページ数をパスと更新時刻でメモ化した。画面操作のたびに全変換PDFを読み直していた処理がなくなる。
- 差分テキストの重複除去を、正規化文字列のバケットによるO(n)判定へ変更し、行構造比較に項目数の上限を設けた。巻全体の比較でタブが停止していた。
- 日本語のOSエラーに対処ヒントが付かなかった。「別のプロセスが使用中」「アクセスが拒否」などの分岐が、日本語判定による早期returnで到達不能になっていた。
- 資料パック未作成のまま原稿を登録すると、内部識別子を含む例外がそのまま表示されていた。手前で「名前を付ける」導線を出すようにした。
- 「更新」で元原稿の更新検知が失敗しても成功と報告していた。確認できなかったことを伝えるようにした。
- ページ並べ替えの保存に失敗した際、画面が未保存の並びを表示し続け、次の操作で一緒に確定されていた。失敗時はサーバの状態へ戻す。
- 差分詳細の取得失敗が、閉じた後や別原稿へ切り替えた後の画面を上書きしていた。成功側と同じ古さガードを掛けた。
- 進捗パネル全体がライブ領域で、0.85秒ごとの更新が読み上げを埋め尽くしていた。節目のみを通知する領域へ分離し、進捗バーに progressbar ロールを付けた。
- サムネイルの選択状態が1pxの枠線色だけだった（背景の差は1.12:1）。太い枠と✓バッジで色以外の手掛かりを追加した。
- RuntimeVersion `2026.08.08.32` へ更新した。

## エラー表示の対処導線と手動クローズ（2026-08-09）

- `showMessage('danger', ...)` がエラーパネルへ分岐する際にアクション配列を破棄していた不具合を修正した。自動PDF作成の失敗通知に指定されていた「原稿を確認」ボタンが表示されるようになった。
- エラーパネルに閉じるボタンを追加した。これまでは成功メッセージの表示かPDF作成の開始まで消えず、読み終えた後も全画面の先頭に残り続けていた。
- アクション実行後はエラーパネルを閉じる。ただし操作の結果として新しいエラーが表示された場合は、その内容を残す。
- RuntimeVersion `2026.08.08.31` へ更新した。

## 作業中心のUI再設計と起動体験の統合（2026-08-08）

- 原稿フォルダ専用ページを廃止し、選択・変更を「原稿を登録・PDF化」へ統合した。左ナビは初回から3工程を同じ位置に表示し、現在地が動かない構成にした。
- 起動ランチャーとアプリのデータ読込待ちを、同じ「ReportBinderを準備しています」画面へ統合した。管理データの読込完了前に空の画面を描画しない。
- 元ファイルの更新警告を最優先にし、PDF作成済みの見た目差分は控えめな要約へ変更した。詳細比較は「変更履歴・比較」に集約し、任意の2世代を選べる役割を明確にした。
- ページ構成を幅広いサムネイル中心の作業面へ整理し、選択後だけ移動操作を表示するようにした。最終PDF画面は未振り分け・変換失敗などの解消タスクを出力操作より先に表示する。
- 英数字はSegoe UI、日本語はBIZ UDPゴシックを基準にし、操作文字・補足文字・クリック領域・キーボードフォーカスを拡大した。
- 完全新規環境を使った独立UX再評価で、初回設定、登録、更新検出、2世代比較、ページ構成、最終出力、終了までを完遂した。
- RuntimeVersion `2026.08.08.30` へ更新した。

## 初めて使う人向けの開始導線と初回設定修正（2026-08-08）

- 高齢でPC操作に不慣れな多忙な管理職と、引き継ぎのない新入社員を想定した独立実機評価を行い、両者が初回設定で離脱する問題を再現した。
- 単一ワークスペースを日本語・英語として連続初期化していた不整合を修正し、選択した原稿フォルダに対するローカル管理領域を一度だけ作成・保存後検証するようにした。
- 初回画面に「複数の原稿を必要な順番で1つのPDFにまとめる」という目的と、原稿を集める・順番を整える・PDFにする流れを表示した。
- 初回の主操作を「はじめる」1つに絞り、原稿フォルダ選択後は一式の命名、作成後は原稿登録へ自動的に進む一本道にした。
- 資料パックを「今回まとめる一式」と平易に表現し、保存先などの技術情報を折りたたみ、重複するエラー表示を除いた。
- PDF作成前に履歴画面を開いていても、画像ハッシュ作成後は要約を必ず再集計し、任意の2世代を直ちに比較できるようにした。
- 登録済み原稿の「作成が必要な分を選択」を廃止し、未選択なら必要分、手動選択中なら選択分を主ボタン1回でPDF化するようにした。
- 担当部署・必須・追加ページ配置を通常画面から「原稿の扱いを変更」へ折りたたみ、各設定が影響する結果を平易に説明した。
- 英数字はSegoe UI、日本語はBIZ UDPゴシックを使う基準に統一し、本文16px、操作部15px、補足13〜14pxへ拡大した。ボタン・入力欄・表の行高も合わせて広げた。
- 起動待ち画面から「準備ができたら画面を開く」を削除し、「操作は不要で、自動的に本画面へ切り替わる」とだけ伝える簡潔な表示にした。
- 左ナビの「ページの順番」を画面タイトルと同じ「ページ構成」に統一した。ページ構成画面は「ページを選ぶ→移動先を押す」だけを主導線にし、表示切替・検索・整列・履歴・キーボード操作を折りたたんだ。
- 未選択時は移動先ボタンを隠し、選択後だけ必要な操作を表示することで初期の情報密度を下げた。
- RuntimeVersion `2026.08.08.13`へ更新した。

## PDF差分の残余リスク検証と安全性・性能強化（2026-08-08）

- 完全重複ページ、異なるスキャンノイズ、全面テンプレート変更とページ増減の併発、220ページ文書の正逆・同一版を実PDFで追加検証した。
- 全面テンプレート変更の共通差をページ距離の基準値から除き、単純な途中挿入後も論理ページを正しく対応付けるようにした。全面書き換えと削除＋追加を画像だけで区別できない場合は、誤って断定せず「対応不確実」として表示する。
- 同一内容のページが複数ある場合は追加・削除件数を保持しつつ、複製位置を特定不能として明示するようにした。
- 64×64画像シグネチャ間の距離計算をPowerShellからC#へ移し、220ページの正逆・同一版662対応を約219秒から約27秒へ短縮した。
- スキャンノイズだけの比較は差分0、ノイズを含む実チェック記入は差分1を維持し、これらを実PDF回帰へ追加した。
- RuntimeVersion `2026.08.08.6`、差分詳細アルゴリズム版 `23` へ更新した。

## 複数ページPDFのページ対応精度強化（2026-08-08）

- 途中ページの追加・削除時に同じ物理ページ番号同士を比較していた処理を、外周を除いた64×64画像シグネチャと文書順序を保つ動的計画法によるページ対応へ変更した。
- 比較元・比較先の物理ページ番号を別々に保持し、「比較元2ページ ↔ 比較先3ページ」のような対応と、文書途中の追加・削除ページを差分画面へ正しく表示するようにした。
- 4系統×4世代・16 PDF・49ページの独立難例評価で、局所差分20/20、構造対応6/6、正逆対応8/8、同一版誤検出0を確認した。
- 実PDF回帰へ、自動フッター番号が変わる途中ページ挿入の正方向・逆方向テストを追加した。
- RuntimeVersion `2026.08.08.5`、差分詳細アルゴリズム版 `22` へ更新した。

## 資料パックへの管理単位統合（2026-08-08）

- 独立した実機UX評価を実施し、初回は既定パック0件から「資料パックを作成」で始める導線へ変更した。ECM/BOD/DMMは内部互換用途に限定し、画面・ひな形一覧・横断進捗へ表示しない。
- 必須にした原稿は、ひな形の旧設定にかかわらず未作成・更新あり・変換失敗・ファイルなしの間は最終PDFを出力できないようにした。原稿一覧と出力前チェックの必須件数も同じ状態を参照する。
- ページ構成は「カードを選択して移動先を指定」を主導線として案内し、選択時の表示を件数だけに簡潔化した。ヘッダーは未作成時の作成操作と提出用PDF状態の役割を明確にした。
- 利用されていなかった資料タイトル・サブタイトル設定を画面から外し、仕上げ設定を出力ファイル名に集約した。
- 日本語管理・英語管理という別ワークスペースを廃止し、すべての資料パックを単一のローカルワークスペースで管理するようにした。
- 起動入口を用途が分かる `資料をPDFにまとめる.cmd` の1本に統合し、旧言語別CMD・VBSと開発用言語別ショートカットを削除した。
- 右上メニューから言語表示と別アプリ起動案内を除き、資料パック切り替えとアプリ終了に役割を整理した。
- 新しい保存先を `data\workspace` とし、旧 `data\ja` / `data\en` は移行せず読み込まない。出力名の初期値は `{packName}_{targetName}_{yyyyMMdd}.pdf` に統一した。
- 表紙・目次・原稿間の区切りページの自動生成を廃止した。これらも通常の原稿として登録し、ページ構成で配置する。
- RuntimeVersion `2026.08.08.4`へ更新した。

## 複数世代のPDF差分・レビュー承認ワークフロー（2026-08-08）

- 任意の過去2版を比較する差分キャッシュを、比較範囲、前後の検知版、前後の描画版、差分アルゴリズム版で厳密に分離した。直前より古い世代でも別世代の結果を誤再利用しない。
- 6世代・全15組の累積変更、行列の追加削除、逆方向比較を画像差分回帰へ追加し、文字主体、履歴書形式、複雑な表、横長ダッシュボードの実PDFコーパスで変更再現率100%、適合率60%以上を継続検証する。
- 差分画面へページ単位の確認済み状態、未確認だけの絞り込み、キーボード操作、CSVへの確認状態出力を追加した。確認状態は比較対象の厳密な版にだけ紐づく。
- 資料パックへレビュー提出、承認、理由必須の差し戻し、再提出、再編集を追加し、操作者・日時・コメント・提出時指紋を監査履歴として保存する。提出後に原稿やページ構成が変わると自動的に更新ありとして扱う。
- ダッシュボードでレビュー未提出、レビュー中、差し戻し、承認対象更新、承認済みを資料パック横断で確認できるようにした。
- ページ構成カードのサムネイル上から選択チェックとドラッグハンドルを外し、右上メニューから安全にアプリを終了できるようにした。
- RuntimeVersion `2026.08.08.2`へ更新した。

## 履歴管理ロジックの整合性強化（2026-08-07）

- 削除済みの検知版、画像ハッシュ、シート別PDFをメモリキャッシュが有効扱いし続けないよう、実体確認と削除時のキャッシュ無効化を追加した。
- 保持原稿を再利用・描画する前にSHA-256を照合し、破損時は同一ハッシュの提出原稿から安全に復旧するようにした。
- 未完成の検知版を履歴一覧と保持世代数から除外し、存在しない検知版・シート別PDFへpin/lease用の空フォルダを作らないようにした。
- 手動pin操作を履歴整理ロック内で処理し、整理との競合で保護対象が消える可能性を防いだ。
- ページ構成履歴の復元を単一の構造ロック内で行い、復元直前のundo版を必須化した。廃止済み出力先は未振り分けへ正規化し、不正値・重複ページID・別言語の履歴を拒否する。
- 履歴専用の実ファイル回帰テストと、任意資料パックの廃止済み出力先・破損履歴テストをCIへ追加した。
- RuntimeVersion `2026.08.07.64`へ更新した。

## 最終出力の出力前チェック（2026-08-07）

- 提出用PDF画面に、必須原稿、変換PDF、未振り分けページ、必須出力先、仕上げ設定をまとめて確認できる出力前チェックを追加した。
- 問題がある項目から原稿・ページ構成へ直接戻れるようにし、最新状態の再確認では原稿更新検出と出力可否判定を一度に更新する。
- 出力を一律に禁止せず、未振り分けなど利用者の判断が必要な注意点を明示したうえで、既存のサーバー側ブロッカーを最終的な安全判定として維持する。
- 出力対象外のページもページ構成画面と同じく未振り分けとして数え、画面間で件数が食い違わないようにした。
- RuntimeVersion `2026.08.07.62`へ更新した。

## 差分レポートCSV（2026-08-07）

- 自動比較と任意2版比較の差分詳細から、原稿、比較日時、前後項目、判定、対応確信度、対応方法、ページ数、説明をCSV出力できるようにした。
- 日本語版Excelでそのまま開けるUTF-8 BOM付きCSVとし、引用符や改行を含む原稿名・説明も安全にエスケープする。
- 出力直後にボタンへ完了表示を出し、ダウンロード操作が受け付けられたことを分かるようにした。
- RuntimeVersion `2026.08.07.61`へ更新した。

## 部署横断の提出期限アラート（2026-08-07）

- 未提出の必須原稿について、期限超過と7日以内の期限を資料パック横断で集計するようにした。
- 期限超過の資料パックを最優先で表示し、状態、件数、次の期限から対象の原稿画面へ直接移動できるようにした。
- 原稿を登録して必要原稿へ割り当てると、期限超過・期限間近の集計から自動的に外れる。
- 原稿登録直後など、プログラムから複数選択した場合にも一括PDF作成アクションが確実に表示されるよう修正した。
- RuntimeVersion `2026.08.07.59`へ更新した。

## Office変換ジョブの安全な中止（2026-08-07）

- 手動のPDF作成中に、進捗欄からジョブの中止を要求できるようにした。
- 現在処理中の原稿は安全に完了し、次の原稿へ進む境界で停止する。完了済みの変換PDF、ページ構成、差分履歴は保持する。
- 中止要求をジョブ状態と独立したマーカーへ保存し、進捗ポーリングでは`cancelRequested`、終了時は`cancelled`を返す。
- 中止API、ジョブ状態、危険なジョブIDの拒否、開始前中止を回帰テストへ追加した。
- RuntimeVersion `2026.08.07.58`へ更新した。

## PowerPoint原稿対応（2026-08-07）

- `.pptx`をExcel・Word・PDFと同じ原稿一覧、登録、更新検出、履歴、物理ページ構成、最終組版へ接続した。
- PPTX内部構造とスライド数をOffice起動前に検査し、暗号化・破損・マクロ混入・空原稿を安全側で拒否する。
- PowerPointを専用の非表示COMワーカーで読み取り専用に開き、Automation Securityを強制してマクロを無効化した。120秒timeout時も、ワーカーが所有するPowerPointプロセスだけを終了する。
- PowerPoint出力を1スライド1PDFへ分割し、スライド挿入・削除を含む自動比較と任意2版比較、ページ移動、最終PDF追跡へ接続した。
- 分割PDFの保存名を`Slide N`の履歴キーへ正規化し、同一レンダリング世代のPDFを履歴比較で確実に再利用できるようにした。
- ひな形の対象形式・必要原稿条件、動作環境診断、形式バッジ、進捗表示へPowerPointを追加した。
- WordとPowerPointの入力履歴を本来の`.docx`・`.pptx`拡張子で保持し、従来履歴の互換読み込みは維持した。
- Chrome実機E2Eで判明した組み込み資料パックのページ移動V2 API識別子不整合も修正した。
- RuntimeVersion `2026.08.07.56`へ更新した。

## 動的出力先・提出管理・自動化運用強化（2026-08-07）

- ひな形の出力先を固定の本体・補足から、1〜12件の任意ID・表示名・必須設定へ一般化した。ページ構成、移動、プレビュー、readiness、最終PDF、共有発行、履歴を同じ出力先定義から生成する。
- ひな形の複製、JSON書き出し・読み込み、既存資料パックへの新版適用プレビューと明示更新を追加した。削除された出力先のページは未振り分けへ戻し、誤った出力先へ残さない。
- 必須提出資料リストと資料パック横断の進捗ダッシュボードを追加し、未提出、未変換、更新あり、未振り分け、最終出力待ちを優先順に確認できるようにした。
- 自動PDF作成に再試行回数、指数バックオフ、必要空き容量、完了・失敗通知を追加した。再試行上限後も原稿が新しい版へ更新された場合は自動復帰する。
- マクロを実行しない既存のOffice安全設定を維持したまま、Excel原稿の`.xlsm`登録・更新検出・変換・履歴保存に対応した。
- 動的3出力先の実PDFワークフロー、ひな形更新、提出条件、進捗集計、自動処理設定・復帰、`.xlsm`候補判定の回帰テストを追加した。
- Chrome実機E2Eで、原稿登録直後に汎用V2状態を旧UIへ適用して登録済み0件と表示される状態ずれを修正した。
- Excelを含む一括変換でExcel起動に失敗しても、Excel原稿だけをエラーにし、PDF・Word原稿の処理を継続する混在形式の障害分離を追加した。
- 未振り分けの新規ページを`ja-unassigned`等の存在しない出力先へ変換せず、常に`none`として扱うよう修正した。
- 組み込み資料パックのページ移動でもV2 APIへ`packId`を渡すよう統一し、旧クライアントの`category`指定もサーバー側で受理するようにした。
- RuntimeVersion `2026.08.07.55`へ更新した。

## 利用者定義ひな形と必須原稿ルール（2026-08-07）

- 資料パックメニューに「ひな形管理」を追加し、Excel・Word・PDFの対象形式、出力先名、必須出力先、新規ページの既定配置、ファイル名、目次、必須原稿の出力条件を作成・編集できるようにした。
- 利用者定義ひな形を言語別の`dataDir\<language>\templates`へJSON保存し、作成・更新・削除APIと版管理を追加した。使用中のひな形は誤って削除できない。
- パック作成時のひな形をスナップショットとして固定し、後日のひな形編集が既存パックへ遡及しないようにした。出力先名と仕上げ既定値も画面・最終PDFへ反映する。
- 原稿ごとの「新規ページの配置」を追加し、原稿指定を優先しつつ、未指定時はひな形の既定先へ新規ページを配置する。既存ページの手動順序・配置は維持する。
- 必須原稿のファイル欠落、未変換・更新、変換失敗をreadiness blockerとして最終出力前に検出する。組み込みECM / BOD / DMMは従来挙動を維持する。
- 回帰テストと実機ブラウザで、ひな形作成・版更新・パック作成・表示名/目次反映・既存パックへの非遡及を確認した。
- RuntimeVersion `2026.08.07.54`へ更新した。

## 任意資料パックのページ構成履歴・復元（2026-08-07）

- ページ移動、並べ替え、ページ設定変更の直前に、資料パック単位のページ構成を自動保存するようにした。
- ページ構成画面に「保存履歴」を追加し、変更内容と対象ページ数を確認して、過去の構成へ限定復元できるようにした。
- `/api/v2/layout/snapshots`、`/api/v2/layout/restore/preview`、`/api/v2/layout/restore`を追加し、別資料パックの履歴やページを復元できないよう検証を追加した。
- 復元前の構成も自動保存し、復元後に「復元の直前」へ戻せるようにした。ページ範囲設定も履歴・復元対象へ追加した。
- 実PDFの任意資料パックで、履歴の分離、復元プレビュー、復元、復元前スナップショットを検証する回帰テストを追加した。
- RuntimeVersion `2026.08.07.53`へ更新した。

## 任意資料パックの共有発行・出力アーカイブ（2026-08-07）

- 任意資料パックの最新PDFを、コピー検証後に提出フォルダーへ切り替える安全な共有発行へ対応した。
- 任意資料パックの最終出力時にPDF、manifest、SHA-256、原稿版・変換環境を含むアーカイブを作成し、出力履歴画面から確認できるようにした。
- `/api/v2/outputs/publish`と`/api/v2/outputs/archives`を追加し、公開・アーカイブ履歴へ`packId`と`targetId`を記録するようにした。
- 実PDFでローカル出力と共有発行PDFのハッシュ一致、アーカイブの永続化・パック分離を検証する回帰テストを追加した。
- RuntimeVersion `2026.08.07.52`へ更新した。

## 任意資料パックの実ワークフロー（2026-08-07）

- 任意に作成した資料パックへExcel・Word・PDFを登録し、変換PDF作成、ページ構成、本体・補足の最終PDF出力まで進められるようにした。
- schema 3の保存同期で原稿の`packId`を維持し、任意パックのページや出力がECMへ混入しないパック単位の絞り込み・並べ替え・再出力判定を追加した。
- `/api/v2/items/reorder`、`PATCH /api/v2/items/{itemId}`、`GET /api/v2/outputs/readiness`、`POST /api/v2/outputs/build`、`POST /api/v2/outputs/file`を追加した。
- 複数ページの実PDFを使い、任意パック作成、登録、取り込み、ページ移動、表紙付き最終PDF、ECMとの出力分離、永続化を検証する回帰テストを追加した。
- RuntimeVersion `2026.08.07.51`へ更新した。

## 動的資料パックUI（2026-08-07）

- ヘッダーの固定ECM / BOD / DMMボタンを、資料パック名を表示する選択メニューへ置き換えた。既存の組み込みパックは同じ処理へ安全に切り替わる。
- 資料パックメニューから新規作成、設定だけを引き継ぐ複製、名称変更、アーカイブ、アーカイブ済み一覧の展開と復元を操作できるようにした。
- 新規作成・名称変更は専用の非ブロッキングダイアログとし、キーボードフォーカス、Escape、背景クリック、モバイル幅に対応した。
- 任意資料パックの選択・管理UIを先行実装し、この時点では未対応の原稿操作を明示して誤操作を防止した。
- RuntimeVersion `2026.08.07.50`へ更新した。

## 任意資料パックAPI（2026-08-07）

- Excel・Word・PDFを扱う汎用の「部門資料パック」テンプレートを追加した。
- `/api/v2/packs`に資料パックの作成、複製、名称変更、アーカイブ、復元APIを追加した。packIdは作成後に変わらず、複製は原稿や出力履歴を持ち込まず設定だけを引き継ぐ。
- 同名パック、空または長すぎる名称、不明なテンプレートを拒否し、アーカイブ済みパックは通常一覧から除外する。`includeArchived=true`で管理用一覧を取得できる。
- 組み込みECM / BOD / DMMは現行画面との互換性のためアーカイブ不可とし、名称と仕上げ設定の変更、複製には対応した。
- 作成・複製・名称変更・アーカイブ・復元・永続化を検証するCIセルフチェックを追加し、RuntimeVersion `2026.08.07.49`へ更新した。

## 汎用資料パックMVP・リリース品質ゲート（2026-08-07）

- Excel、Word、PDFを同じ資料パックで扱うschema 3、共通原稿アダプター、形式横断の履歴差分、表紙・目次・区切り・ブックマークを含む最終組版を完成した。
- Office診断、schema 1/2移行、障害復旧、共有フォルダー配布、大容量・混在原稿ベンチマークをリリース受け入れ条件へ追加した。
- すべてのpull requestとmainへのpushで、Windows PowerShell 5.1構文、第三者依存物、selfcheck、差分回帰、最終組版、運用復旧、3種類の配布物を検証するGitHub Actionsを追加した。
- 手動のリリースワークフローから、検証済みのオフライン版・オンライン版・共有フォルダー版をGitHub Actions artifactとして作成できるようにした。
- RuntimeVersion `2026.08.07.48`へ更新した。

## ページ構成画面の操作性改善（2026-08-06）

- PDFページを見ながら整理できるサムネイル表示を既定にし、一覧性を優先する詳細表示と切り替えられるようにした。
- 選択数と移動先をまとめた追従操作バーを追加し、複数ページを本体・補足・未振り分けへ少ない操作で移動できるようにした。
- 各出力先を折りたためるようにし、空の出力先もドロップ先として残した。ドラッグ中は挿入位置を表示し、折りたたまれた移動先は自動で展開する。
- ページ構成の直前操作を「元に戻す」ボタンまたは完了通知から復元できるようにし、並べ替え・一括移動・ドラッグ移動の誤操作から戻れるようにした。
- 「変更分のみ」に該当ページがない場合の空状態を追加し、解除方法を画面内で案内するようにした。
- キーボードのAlt＋上下左右キーによるページ移動、選択状態を維持した表示切替、表示時だけPDFを描画する遅延サムネイル生成を追加した。
- ページ名・Excel名・シート名を横断する検索と、サムネイルの小・標準・大の表示サイズ調整を追加した。検索中の件数を各出力先へ表示し、「表示分を選択」は検索結果だけを対象にする。
- Alt＋左右キーで未振り分け・本体・補足をまたいでページを移動できるようにし、Alt＋Shift＋左右キーでは端の出力先へ直接移動できるようにした。
- サムネイルカード内にページ設定エディターを追加し、詳細表示へ切り替えずにページ名とページ番号の表示方法を編集できるようにした。保存前の取消、空のページ名の拒否、保存後8秒間の取り消しに対応した。
- 各設定ボタンには対象ページ名を含む読み上げ名と編集欄との関連を設定し、設定を開いてもカード選択やPDFプレビューが誤作動しないようにした。
- PDFプレビュー内に同じ出力先の前後ページ移動と現在位置を追加し、左右矢印キーでもページを確認できるようにした。
- 表示中のページをプレビューから本体・補足・未振り分けへ直接移動できるようにし、完了通知から元に戻せるようにした。モーダル上でも通知を操作でき、閉じた後は再描画後の対象カードへフォーカスを戻す。
- 14ページ・3出力先の実データ相当環境で、表示切替、検索、一括選択、サイズ保持、キーボード移動、取り消し、折りたたみ／再展開を実ブラウザー検証した。さらにカード内の編集・取消・保存・空欄拒否・保存後の取り消しを検証した。
- 異なる内容の実PDF 4ページで、PDF表示、前後移動、位置表示、矢印キー、出力先変更、通知からの取り消し、フォーカス復帰を実ブラウザー検証し、RuntimeVersionを`2026.08.06.35`へ更新した。
- ページ構成の操作履歴を最大20件保持し、ドラッグ、一括移動、キーボード移動、プレビュー内移動、Excelシート順への整列を複数段階で「元に戻す／やり直す」できるようにした。
- `Ctrl+Z`、`Ctrl+Shift+Z`、`Ctrl+Y`に対応し、新しい変更を加えた場合は古いやり直し分岐を破棄する。カテゴリを切り替えた場合は別カテゴリへ履歴を持ち越さない。
- 実ブラウザーで2回の変更、2回の取り消し、2回のやり直し、ショートカット、履歴分岐を検証した。一括移動後に押した移動先ボタンだけ有効へ戻る状態ずれも修正し、RuntimeVersionを`2026.08.06.36`へ更新した。
- ページカードを矢印キーとHome／Endで移動し、Spaceで選択、`Ctrl+A`で表示中のページを一括選択、Escapeで選択解除できるようにした。検索・表示切替・並べ替え後も対象カードへフォーカスを戻す。
- キーボード操作ガイドと明瞭なフォーカスリングを追加し、Alt＋矢印で並べ替えた直後の表示番号と読み上げ用ページ番号を同期した。サムネイル表示と詳細表示を実ブラウザーで検証し、RuntimeVersionを`2026.08.06.37`へ更新した。

## 実機Word多様文書の差分局所化（2026-08-05）

- 実機Microsoft Word 16.0で、表を含まない2ページ業務文書、日本式履歴書、2段組み職務経歴書をPDF化し、文字置換・段落追加・行間変更・文字色変更・連絡先変更・職歴行追加・罫線変更・箇条書き追加を検証した。
- 複数ページの文字行を文書全体で照合し、追加段落だけを1領域へまとめ、改ページで次ページへ移動した既存文章は変更表示しないようにした。
- 2段組み文書では文字配置から列境界を求め、影響を受けた列だけを照合して追加箇条書きを1領域へ局所化した。
- 同じ文字内容の位置変化量を行ごとに比較し、行間や罫線による後続レイアウト移動を変位の開始箇所1領域へ集約した。
- 文書全体・レイアウト比較では上下5%の反復ヘッダー／フッターを除外し、本文の改ページとページ家具の変更を混同しないようにした。
- 差分詳細アルゴリズム版を`20`、RuntimeVersionを`2026.08.05.31`へ更新した。

## 実機Excel複合帳票の差分局所化（2026-08-05）

- 実機Microsoft Excel 16.0で、複数表・説明文・KPI・レビュー表・点線／破線／太線／二重線を含むA4横帳票をPDF化し、ブラウザPDF.js経路で回帰検証した。
- 左右に表が並ぶページでも、画像側が確認した表帯へPDF文字行LCSを限定し、行追加・逆向き削除を対象行1領域へ集約するようにした。
- 行高変更による印刷倍率変化で表上端の罫線を失わないよう、横罫線探索を表の公称行高に合わせて拡張した。
- 複数表が連結して見える帯を単一表の列追加と誤認しないよう、列構造モデルの罫線数を制限した。
- 離れた濃色タイトルを表ヘッダーとして採用せず、列幅変更の強調を対象表・対象列だけに限定した。
- 差分詳細アルゴリズム版を`19`、RuntimeVersionを`2026.08.05.30`へ更新した。

## 比較画像生成の一括・並列化（2026-07-30）

- 変更シートごとに新旧PDF用のJavaプロセスを2回起動する処理を廃止し、対象シート全部を1つのJVMで一括画像化するようにした。
- 独立したPDFの画像化を最大4並列で実行し、複数シート比較の待ち時間を短縮した。
- 変更マスク・枠線・M/A/Dタグを4枚のPNGとして保存する処理を廃止し、領域JSONからブラウザ上で描画するようにした。比較ページごとの保存画像は新旧2枚だけになった。
- 差分キャッシュのアルゴリズム版を8へ更新し、旧方式の未完了キャッシュを再利用しないようにした。

## 比較速度・誤検知・変更表示の改善（2026-07-30）

- 比較用ラスタライズを150dpiから120dpiへ下げ、元のPNGを再圧縮せずキャッシュへ再利用することで、比較画像の作成時間と一時メモリを削減した。
- 全画素SHA-256の完全一致だけでなく、抽出文字ハッシュと32x32知覚ハッシュを組み合わせる判定を追加し、アンチエイリアス等の人の目に見えない差を「変更」から除外した。
- 新旧画像の最大4pxの位置ずれを自動補正してから差分を取り、ページ全体が変更扱いになる誤検知を抑制した。
- 近接する変更領域を統合し、Mタグは1ページ12領域までに限定した。密集時は枠線だけを表示する。
- 広範囲かつ低密度の差分は全ページを囲まず、判定不能として左右比較を案内するようにした。

## UI導線・カテゴリ別出力名の修正（2026-07-30）

- 任意2時点の視覚比較を「最終PDF」画面下部から独立した「履歴・比較」メニューへ移し、主要機能として先頭に表示した。
- 未登録Excelの「表示分を選択」「選択解除」「選択したExcelを登録」を一覧下部から一覧上部の操作バーへ移した。
- BOD/DMMの最終PDF名が元Excel名のECM表記を引き継ぐ問題を修正し、選択中カテゴリをファイル名へ必ず反映するようにした。
- UI配置とカテゴリ別ファイル名の回帰検査をselfcheckへ追加した。

## レビュー修正（2026-07-30・第6回）

- PDFページ画像解析を`-Djava.awt.headless=true`付きで起動するようにした。タスク実行・リモートセッション・画面のない検証環境でもAWTがデスクトップ接続を要求せず、画像ハッシュ生成が安定する。
- 差分画像生成の実行失敗を、比較結果としての「判定不能」と分離した。従来はPDF不足・画像化失敗・書込み失敗なども`unknown`へ変換され、差分詳細全体が`ready`になって再試行できなかった。生成失敗は`failed`として保持し、全体再試行または対象シートの再選択で再生成できるようにした。
- 変更なしシートの遅延生成が失敗した場合も、全体の再試行対象へ含めるようにした。全体再試行時にも既存の差分詳細を識別子検証付きで読み戻し、`failed`状態を失わない。従来の全体処理は既存詳細を捨てたうえで変更なしシートを常に除外するため、一度失敗すると復旧経路がなかった。
- 別シートの生成ジョブへ相乗りした後、対象シートが`failed`または`deferred`のままなら再要求するようにした。自分で起動したジョブが失敗した場合は無駄な連続再実行をせず、その場で失敗表示へ確定する。絞り込みで失敗シートが選択された場合も再試行できる。
- 差分詳細の再試行ボタンが`MouseEvent`オブジェクトを`sheetKey`としてAPIへ送っていた不具合を修正した。クリック時は引数なしで全体再試行を呼ぶ。
- 失敗後に再試行が成功しても古いエラーメッセージが画面へ残る問題を修正した。描画のたびに状態欄を初期化し、現在のシート／ページ状態だけを表示する。
- 保存済み差分詳細を再利用する際、アルゴリズム版だけでなくworkbook・scope・比較元／比較先のsnapshotId・versionIdをすべて検証するようにした。短縮キャッシュキーの偶発衝突や不整合ファイルを別比較として表示しない。
- 差分PNGを返すAPIでも同じ完全一致検証を行い、詳細JSONが別比較・旧形式・破損状態なら画像を配信しないようにした。
- 差分キャッシュキーを32bit相当（8桁）から64bit相当（16桁）へ拡張した。短縮後の実パス長はMAX_PATH内に収まりつつ、任意2版比較を繰り返した際の衝突余地を大幅に下げた。
- キャッシュ形式変更に伴い差分詳細アルゴリズム版を`6`へ更新した。
- 差分ジョブが終端状態なのに`diff-detail.json`が欠けている場合、`generating`を返し続けるのではなく再試行可能な`failed`を返すようにした。
- 単一シートの遅延生成が外側の例外で中断した場合も、詳細全体と対象シートを`failed`へ確定し、「生成中」のまま残さないようにした。

## レビュー修正（2026-07-30・第5回）

- 遅延生成した1シートが差分詳細全体を「作成済み」にしていた問題を修正した。
  全シート生成が途中で失敗したあとに「変更なし」シートを1件開くと、`diff-detail.json` の
  `status` が `ready` へ上書きされ、`pending` のまま残ったシートを二度と生成できなく
  なっていた（`Start-DiffDetailJob` が「差分詳細は作成済みです」を返し、画面にも
  再試行ボタンが出ない）。完了時の状態は未生成シートの残件数から決めるようにした。
- 変更領域を「文字1つごと」ではなく「まとまり」で切り出すようにした。従来は連結領域の
  抽出後に余白を足していたため、数値1つの書き換えで桁ごとに5個の枠が重なって表示され、
  行がずれたページでは1ページあたり数千個の枠になっていた（実測3,772個）。
  変更画素を余白幅で膨張させてから連結判定するよう変更し、同じ検証で22個になった。
- 1ページあたりの強調領域を400個で上限とし、超えた場合は枠を描かずに
  「変更領域が多すぎるため、領域の強調を停止しました」と表示するようにした。
- ページ正規化の `DrawImageUnscaled` を明示的な転送先矩形付き `DrawImage` に置き換えた。
  `DrawImageUnscaled` は名前に反して元画像のDPIメタデータで拡縮するため、
  PDFBoxがPNGの解像度情報を書かない構成では、ページが拡大されて右下が欠けた画像が
  保存されるおそれがあった。
- 差分画像のファイル名を短縮した（`page-0001-before-overlay.png` → `0001-bo.png`）。
  実ワークスペースでのキャッシュパスは253文字で MAX_PATH まで7文字しか余裕が
  なかったため、236文字（余裕24文字）にした。
- キャッシュのレイアウトと領域抽出が変わるため、差分詳細アルゴリズム版を `5` へ更新した。
  適用後の初回表示では差分画像を作り直す。
- 上記すべてを `selfcheck.py` の回帰検査へ追加した。あわせて `DiffImageEngine.cs` が
  C# 5 互換のまま（`=>` / `$"` / `?.` / `out var` を使わない）であることも検査する。
  Windows PowerShell 5.1 の `Add-Type` は C# 5 の CodeDom でコンパイルするため。


## 履歴視覚比較の整合性・同時実行修正（2026-07-30）

- 画像ハッシュの世代と画面表示用content PDFの世代を厳密一致させ、別versionへの
  暗黙フォールバックを廃止した。比較可否はmanifestの古い保持フラグではなく、
  画像ハッシュと同一versionの実PDFが全シート分存在するかで判定する。
- 履歴一覧へ視覚比較可否・推奨version・利用不可理由を返し、比較資産が不足する版は
  選択できないようにした。
- 自動比較・履歴比較の保存結果をscope、比較元／比較先のsnapshotId・versionIdの
  完全一致で検索し、比較メタデータと画像キャッシュの両方を分離した。
- GETの差分詳細取得では比較結果を保存せず、POSTの準備処理でのみ保存するよう変更した。
  保存後の再読込・識別子検証に成功してから履歴イベントを記録する。
- 比較組合せ単位の共有ロック、snapshot/content PDFのleaseとheartbeat、終了時finally解放を追加した。
  履歴整理・content PDF整理とも同じ保守ロックを使い、lease作成との競合を防止する。
- 次回の自動比較基準に加え、直近の変更バッジが参照する比較元・比較先のsnapshotと
  同一versionのcontent PDFを専用pinで保護し、保持世代数を超えても判定対象と表示対象が残るようにした。
  旧基準の同一世代PDFが欠けている場合は、保存済みExcelからハッシュとPDFを一組で再生成し、
  今回版・前回版の両方を実ファイルで検証してから比較結果を保存する。
- 自動比較結果も保存後に再読込し、scopeと比較元／比較先のsnapshotId・versionIdを検証してから
  完了イベントを記録する。保存不整合がある比較を変更バッジへ載せない。
- 変更なしシートは初回の一括画像化から除外し、選択時だけ生成するようにした。
  別シートの生成ジョブが実行中だった場合は完了後に対象シートを再要求する。
- 差分詳細アルゴリズム版を`4`へ更新し、上記の整合性・排他・遅延生成をselfcheckへ追加した。

## 任意の過去2版の視覚比較（2026-07-30）

- 履歴画面で比較元と比較先を選び、保存済みの任意2版を既存の差分詳細ダイアログで
  左右比較・重ね合わせ・領域移動できるようにした。比較結果が無い組合せも、
  保存済みの画像ハッシュとcontent PDFからオンデマンド生成する。
- 履歴比較は短い専用キャッシュ名へ保存し、自動比較の
  `comparison-baseline.json`や最新PDFの変更バッジを変更しない。
- 自動比較と履歴比較で同じ2版を選んでも生成済み詳細が混線しないよう、
  キャッシュキーへ比較scopeを含め、差分詳細アルゴリズム版を`3`へ更新した。
- MAX_PATH対策後の短縮比較ファイル名をJSON内の`baselineSnapshotId`で検索するよう修正し、
  旧ファイル名形式を前提にした履歴差分検索の不整合を解消した。
- 履歴画面に選択中の比較元／比較先日時と入れ替え操作を追加し、日英それぞれで
  同じ期間の変更を担当者が素早く見比べられるようにした。

## セキュリティ強化（2026-07-30）

- `server.ps1`をランチャーを介さず単独起動した場合も、32バイトの
  `RandomNumberGenerator`からURL安全なトークンを生成するようにした。
- クエリ、ヘッダー、Cookieから受け取るトークンは、UTF-8文字列のSHA-256値を
  全バイト走査する固定時間比較で照合するようにした。PowerShellの既定の
  大文字小文字を区別しない`-eq`には依存しない。
- 上記2点を`selfcheck.py`の回帰検査へ追加した。
- 初回PDF作成直後など、まだ比較基準または比較結果が無い状態で差分詳細APIを開いた場合に、
  空の版IDを履歴パスへ渡してHTTP 400になる問題を修正した。正常な`unavailable`状態を返す。
- PDFジョブ完了後の画像解析と、直後に開始された次版PDF作成が重なった場合でも、
  現在版manifestの`previousSnapshotId`と保存済み画像ハッシュから比較基準を回復し、
  比較結果が欠落しないようにした。
- 各レンダー版へ`comparison-analysis.json`を保存し、比較できなかった場合も
  `unavailable`の理由を後から診断できるようにした。
- 深い提出フォルダでは比較JSONのパスが260文字を超え、判定は完了しているのに
  保存だけ失敗する問題を修正した。比較ファイル名、差分キャッシュキー、
  シートキー、画像格納フォルダをハッシュ短縮し、PowerShell 5.1でも扱える長さにした。
  キャッシュレイアウト変更に伴い差分詳細アルゴリズム版を`2`へ更新した。
- JSONの原子的保存で使用するGUID付き一時パスがMAX_PATHを超えた場合も、
  例外を捕捉して短い最終パスへの共有直接書き込みへ縮退するようにした。
- PDFBoxが生成するラスタ作業画像を深い履歴キャッシュ内ではなく、
  短いWindows TEMP配下へ作成し、完成PNGだけを履歴へ保存するようにした。

## 差分詳細・視覚比較（2026-07-29）

- 登録済みExcelの変更バッジをボタン化し、前回版と現在版を同じ画面で確認できる
  差分詳細ダイアログを追加した。
- シート単位の変更・追加・削除・判定不能・変更なしを一覧化し、件数バッジで絞り込めるようにした。
- content PDFを150 DPI・RGBで画像化し、RGB最大差18、微小ノイズ除去、16px以上の
  連結領域、6px余白の条件で変更領域を生成する機能を追加した。
- 変更は琥珀色＋実線＋M、追加は青色＋実線＋A、削除は赤色＋破線＋D、
  判定不能は灰色＋点線＋?で表示する。強調のON/OFFと0%／25%／50%の濃度に対応した。
- 左右のスクロールと倍率を同期し、左右比較／重ね合わせ、全体表示／幅合わせ、
  ページ移動、前後の変更領域への移動に対応した。
- 差分画像はバックグラウンドジョブで作成し、現在版・比較基準版・アルゴリズム版に紐づけて
  履歴内へキャッシュする。シート単位で進捗を保存するため、ダイアログを閉じても処理は継続する。
- `GET /api/history/diff-detail`、`POST /api/history/diff/prepare`、
  `GET /api/history/diff-page`を追加した。画像取得は履歴インデックスとmanifestを照合し、
  任意パスを受け付けない。
- 1100px以下では前回版／現在版をタブ切替にし、Esc、フォーカストラップ、起点への
  フォーカス復帰、色以外の記号・枠線・凡例を実装した。

## Codexレビュー修正（2026-07-29）

- 履歴・差分・自動処理APIから受け取る `workbookId` / `snapshotId` / `versionId` を、
  保存先の単一パス要素として検証するようにした。区切り文字、ドライブ指定、`..`、
  制御不能な長さを拒否し、管理フォルダ外の参照・書き込みを防止する。
- 履歴の保護情報を書き込めなかった場合に、成功応答を返さずエラーとして通知するようにした。
- ローカルHTTPサーバーのリクエスト本文を1 MiBに制限し、不正な `Content-Length`、
  本文が途中で切れたリクエスト、過大なリクエストを HTTP 400 で拒否するようにした。
- 画面側のAPI共通処理で、`FormData` / `Blob` をJSON文字列化して空の本文にしてしまう
  不整合を修正した。
- 「Excel登録・PDF作成」の変更列で、追加シートが表示されず「変更分のみ表示」からも
  漏れていた問題を修正した。ブック単位では `追加 Nシート`、ページ単位では `追加` と表示する。
- 1440px前後の画面では未登録Excelを約1/3、登録済みExcelを約2/3の横配置にし、
  情報量の多い登録済み表へ広く割り当てた。1320px以下では縦配置に戻し、変更列は230px確保する。
- Excel一覧ではファイル名を主情報として省略せず折り返し表示し、更新日時／PDF作成日時は
  ファイル名の下へ補助情報として配置した。横幅を日時列に奪われず、ファイルを識別しやすくした。
- 登録済みExcelの「変更」と「状態」を「PDF状況」へ統合した。PDF作成前は
  「差分は作成後に確認」、作成後だけ「前回PDFとの差分」を表示し、古い差分結果と現在の
  PDF作成要否が矛盾して見える問題を解消した。
- 上記の保存パス検証とHTTP本文制限を自己診断の回帰チェックへ追加した。

## 実運用での発見 その4（2026-07-29・P0）

**日本語サーバーを起動すると、稼働中の英語サーバーが強制終了される**（逆も同様）。
E側で作業中にJ側を立ち上げると画面が「Failed to fetch」になり、PDF作成中なら結果も失われる。

`launch.ps1` の `Stop-StaleUiServerProcesses` は、起動前に古いUIサーバーを掃除する。
その判定が「コマンドラインに server.ps1 を含むか」だけで `-Mode` を見ていなかったため、
別言語の稼働中サーバーまで `Stop-Process -Force` の対象になっていた。
`-AutoSchedulerPath` の子プロセスも除外されておらず、巻き添えで落ちていた。

- 停止対象を「同じ `-Mode` のUIサーバー」に限定
- `-RenderJobPath`（PDF作成の子）に加えて `-AutoSchedulerPath`（自動処理の子）も除外
- 自己診断に回帰チェックを追加

なお `app\tools\stop-reportbinder.cmd` は「ReportBinderを止める」ための道具なので、
従来どおり両言語をまとめて停止する（仕様どおり）。

## 実運用での発見 その3（2026-07-29・P0）

日本語ワークスペースで全17件のPDF作成が「引数の型が一致しません」で失敗する。
英語側は正常。違いは **J側は pages が0件で、ページを新規追加する経路を通る** こと。

`pages` は JSON 由来の `PSCustomObject` で構成されるのに、
新規ページだけ `[ordered]@{}`（`OrderedDictionary`）で作っていたため、
コレクション内に2種類の型が混在していた。
Windows PowerShell 5.1 はこの混在を `Sort-Object` などの型比較で
`ArgumentException`（Argument types do not match）として弾くことがある。
PowerShell 7 では再現しないため、AST解析でも pwsh 実行でも検出できなかった。

- 新規ページを `[pscustomobject][ordered]@{}` に変更し、`pages` の要素型を統一
- `Renumber-VolumeOrder` / `Apply-DefaultNumberingPerVolume` のソートキーを型安全化
  （`[double](Get-DataProperty $_ 'order' 0)` と `[string](Resolve-PageId $_)` に統一。
  後者は `$_.order` を直接参照しており、プロパティ欠損時に型が揺れていた）
- `Get-ErrorDetail` を強化。従来は `Exception.ToString()` が取れないと
  メッセージ1行だけになり原因追跡が不可能だった。
  例外の型・HResult・.NETスタック・内部例外（5段まで）・発生行・errorId を、
  取得できたものから必ず積むようにした

## 実運用での発見 その2（2026-07-29・P0）

`renders` フォルダは作られるのに `visual-hashes.json` が一切書かれない、という状態が残っていた。
手動で `PdfPageAnalyzer` を実行すると exit=0 でハッシュも正常に出るのに、
アプリ経由では必ず失敗扱いになる。

原因は **Windows PowerShell 5.1 の `2>&1` と `$ErrorActionPreference = 'Stop'` の組み合わせ**。
PS5.1 ではネイティブコマンドの stderr を `2>&1` で取り込むと ErrorRecord としてパイプラインに流れ、
`Stop` 設定下では NativeCommandError の終了エラーになる。
PDFBox は日本語フォントを含むPDFで
`Format 14 cmap table is not supported and will be ignored` を stderr に出すため、
解析が成功して `result.json` を書いていても、その手前で例外になり `catch` へ落ちていた。
結果、`render-manifest.json` だけが書かれ `visual-hashes.json` は書かれない。

- `Invoke-NativeCapture` を追加。呼び出しの間だけ `ErrorActionPreference` を `Continue` にし、
  終了コードと出力を文字列として回収する
- Java を呼ぶ5箇所すべてを置き換え
  （`PdfPageAnalyzer` / `BatchPdfSplitter` / `ReportPdfComposer` 2経路 / `java -version`）
- 自己診断に、`Invoke-NativeCapture` 以外の生の `2>&1` を禁止する回帰チェックを追加

Linux の検証環境では PowerShell を経由しないため再現しなかった。
PS7 はこの挙動を廃止しているため、AST解析や pwsh での実行でも検出できない。

## 実運用での発見（2026-07-29・P0）

有効化後の実機確認で、**画像ハッシュの解析が一度も実行されていない**ことが判明した。
`structure.json` の `lastRenderedSnapshotId` は正しく入るのに
`input-history\<workbookId>\<snapshotId>\renders` が作られず、
「変更」列が永久に空欄のままになる。エラーは出ない（解析失敗はPDF作成を止めない設計のため）。

原因は `Render-Workbook` 末尾の `$Script:PendingAnalysis` の扱い。
共有Excelを使う一括ジョブ（`KeepExcelOpen = $true`）では、
`Invoke-RenderJobFromFile` が `$deferredAnalyses` へ回収してから
Excelとロックを解放したあとに解析する設計だが、
`Render-Workbook` が `KeepExcelOpen` に関係なく `$Script:PendingAnalysis = $null` を
先に実行していたため、呼出元が回収する時点では常に `$null` だった。
画面からのPDF作成は必ずこの一括ジョブ経路を通るため、解析が一切走っていなかった。

- `KeepExcelOpen` のときは `$Script:PendingAnalysis` を消さずに呼出元へ渡すよう修正
- 未使用のまま残っていた `Invoke-DeferredAnalysis`（どこからも呼ばれていない死んだ関数）を削除
- 自己診断に、クリアが `KeepExcelOpen` ガードより後にあることの回帰チェックを追加

**適用後、各Excelを1回ずつPDF作成し直して比較基準を作り直す必要がある。**
それまでに作られた検知版には `renders` が無いため比較できない。

## 承認反映（2026-07-29）

Phase 0（入力履歴の業務承認）が確定したため、配布物の既定値と手順書を更新した。コードの変更はなし。

- `app\default-config.json` の `inputHistory.retainSourceVersions` を 2 → **5** に変更。
  正式版に使っていない検知版を最新5世代まで残す。
  正式版に使った版は pin により世代枠の対象外で、`sourceRetentionDaysAfterBuild` は `null`
  （期間では消さない）のままとし、不要になった時点で手動整理する。
- `docs\INPUT_HISTORY_POLICY.md` を追加。承認事項4件、`policy.json` の作り方、
  保持の仕組み（世代数と pin の違い）、設定値の配り方、手動整理の手順、段階的な有効化をまとめた。
- 設定値は共有側ではなく `%LOCALAPPDATA%\ReportBinder\config.json` を参照する。
  既に起動したことのあるPCでは既存キーが上書きされないため、
  `default-config.json` の変更は未起動のPCにだけ反映される点を明記した。

## レビュー修正 第4回（2026-07-29）

第3回修正版の再レビュー。第3回の修正内容はいずれも妥当で、
PowerShell 7.4.6 での構文解析（全7本エラー0）も通過した。以下2点のみ修正した。

- **配布ZIPの日本語ファイル名が文字化けする問題を修正（最優先・配布不能レベル）**。
  第3回のZIPはエントリ名をUTF-8バイトで書きながら
  「言語エンコーディングフラグ（汎用目的ビット11）」を立てていなかったため、
  日本語Windowsのエクスプローラーが名前をCP932と誤解し、
  `日本語管理.vbs` / `英語管理.vbs` / `docs\ReportBinder_UIUX改修指示書_V4.md`
  の3件が文字化けした状態で展開される。起動用VBSが読めなくなる。
  展開後の `selfcheck.py` もこの3件を「missing」として検出する。
  `app\tools\package-release.ps1` の `New-Utf8Zip`（V5-P0(#1)で導入済みの正規パッケージャー）
  と同じ方式で再パッケージした。
  自己診断に、パッケージャーが `New-Utf8Zip` を使い続けているかの回帰チェックを追加。
- 自動スケジューラーの停止確認間隔を 100ms → 500ms に変更。
  `ControlPath` は共有ドライブ上にあるため、100ms間隔だと利用者1人あたり毎秒10回の
  SMB `Test-Path` が常時発生する。親側は `WaitForExit(2500)` で待つため、
  500msでも正常終了と `finally` の後片付けは間に合う。

### 検証（第4回）

- PowerShell 7.4.6 で全7本の `.ps1` を構文解析：**エラー0**（第3回版・第4回版とも）
- `selfcheck.py`：合格。**ZIP再展開後も合格**（第3回のZIPは文字化けにより不合格）
- `app.js` 構文確認：合格 ／ 全 `.ps1` の UTF-8 BOM：確認済み
- ZIPエントリの UTF-8 フラグ（bit11）が非ASCII名で立っていることを確認
- **Windows上のExcel COM・VBSを含むE2E試験は未実施**

## レビュー修正 第3回（2026-07-29）

第2回修正の再レビューで見つかった後処理・キャッシュ境界の不具合を修正。データ形式の変更はなし。

- 履歴容量の60秒キャッシュを `language + input-historyルート` 単位に変更し、dataDir切替直後に旧ワークスペースの容量を表示する問題を修正
- `Reset-ConfigCaches` から履歴容量キャッシュも無効化
- TCP listenerの生成・開始失敗時でも自動スケジューラーを必ず停止するよう、サーバー初期化全体を `try/finally` 内へ移動
- スケジューラー子プロセスの10秒Sleepを100ms単位の停止確認へ変更。正常終了を待ってから最終手段としてKillするため、ロック解放・状態復旧の `finally` が通常は実行される
- スケジューラーの制御JSONと `.stop` ファイルを終了時・起動失敗時に削除し、`schedulerRunning` を実プロセスの生存確認で返すよう修正
- 巻き戻し済みトランザクションの `state\final-backups\<transactionId>` が無期限に残る問題を修正。巻き戻し完了時に削除し、30日ジャーナル掃除時にも安全側で削除
- PDF.jsの説明を `README.md`、`THIRD_PARTY_NOTICES.md`、`docs/THIRD_PARTY_SETUP.md`、`docs/API.md` まで統一
- 自己診断へ上記の回帰チェックを追加

## レビュー修正 第2回（2026-07-29）

ホットパスのディスクI/Oを中心とした性能不具合の修正。機能・データ形式の変更はなし。

- `Get-AppConfig` が呼ばれるたびに `config.json` を書き戻していた問題を修正。
  `Get-Paths` → `Get-WorkspacePath` 経由でほぼ全関数から呼ばれるため、
  検知版を1件参照するごとにローカル設定ファイルへの書き込みが発生していた。
  既定値を実際に補完したときだけ書き、結果は2秒キャッシュする（`Reset-ConfigCaches` で無効化）
- `Get-Paths` が共有ドライブ上の `common\paths.json` を毎回読み直していた問題を修正（同キャッシュ）
- `Get-WorkspacePolicy`（`Test-InputHistoryEnabled` / `Test-SourceRetentionEnabled`）を5秒キャッシュ。
  `Write-HistoryEvent` などのホットパスから多数回呼ばれていた
- `Get-InputHistorySizeMb` が `input-history` 配下を全再帰列挙していた問題を修正。
  `/api/state` のポーリングごとに実行されていたため60秒キャッシュとし、掃除後に強制再計測する
- `Get-LatestComparison` がブック1件ごとに `structure.json` を丸ごと読み直していた問題を修正。
  読込済みのブックを受け取れるようにし、`/api/state` の90回の再読込を1回にした
- `Stop-AutoSchedulerProcess` が定義のみで一度も呼ばれていなかった問題を修正。
  サーバー終了時に呼び出す（従来は親PID監視のみで、子プロセスが最大10秒残り `auto-owner` ロックを保持していた）
- 完了済み・巻き戻し済みの出力トランザクションのジャーナルが無期限に溜まる問題を修正（30日で削除。
  `manual-recovery-required` は担当者が確認するまで残す）
- `app/web/pdfjs/README.md` の記述を実装に合わせて訂正。
  V5時点のプレビューは blob URL + iframe（ブラウザ内蔵ビューア）であり、PDF.js は使用していない。
  同梱ファイルは今後の視覚差分表示に向けた先行配置である旨を明記した
- 自己診断に上記の回帰チェックを追加

### 検証（第2回）

- PowerShell 7.4.6 で `server.ps1` ほか全 `.ps1` の構文解析：**エラー0**
- ロック用スクリプトブロックの動的スコープ衝突をAST走査で検査：実害のある取り違えなし
- 設定・パス・ポリシーのキャッシュを実行して動作確認（初回作成／定常読取で書き込みなし／
  キー欠落時の補完と書き戻し／`Save-AppConfig` での無効化／承認フラグの反映）
- `PdfPageAnalyzer` を実PDFで実行し、同一PDF＝同一ハッシュ・1行変更で別ハッシュを確認
- **Windows上でのExcel COM / VBS起動を含むE2E試験は未実施**

## レビュー修正（2026-07-29）

- カスタムTCPサーバーのクエリ文字列が PowerShell により配列へ展開され、正しいトークンでも API が HTTP 403 になる問題を修正
- レンダリングロックのラッパーが PowerShell の動的スコープで自身を再帰実行し、ブックロックへ自己衝突する問題を修正
- Windows PowerShell 5.1 で `List[object]` を `@()` 変換するとページ追加時に「引数の型が一致しません」になる問題を修正
- レンダリング失敗ログへ例外型・PowerShellスタック・発生行を記録し、原因を追跡できるように改善
- `ReportPdfComposer.jar` を再ビルドし、欠落していた `PdfPageAnalyzer.class` を同梱
- 再ビルド用の一時 `classes` フォルダーを配布物へ残さないように修正
- リリースZIP名を `ReportBinder_V5_*` に修正
- 自己診断に、クエリ文字列コレクション・解析クラス同梱・一時クラスフォルダーの回帰チェックを追加

## Stage 1 — 基盤

Phase 1A 以降が依存する土台のみ。**この時点では動作は V4.1 と同じ**で、履歴・差分・自動化の機能はまだ入っていない。

### 決定事項

- Excel COM は**言語ごとに1ジョブ**に制限する（`locks\render-engine.lock`）。

### server.ps1

| 区分 | 内容 |
|---|---|
| ハッシュ表現 | `Normalize-FileHash` を追加。`New-Sha256`（64文字・大文字・prefixなし）と `Get-Sha256Text`（`sha256:`+小文字）の混同を防ぐ |
| ID規則 | `New-RbId` / `New-RbVersionId` / `New-UniqueDirectory` を追加。`versionId` を秒単位からミリ秒+GUID8へ変更（同一秒の2回処理で世代フォルダが混ざる問題） |
| ロック | `Try-AcquireLockHandle` / `Release-LockHandle` を追加（`Invoke-WithLock` は body 終了でハンドルを閉じるため、tick をまたいだ保持ができない）。`Invoke-WithRenderLock` を追加し、取得順序を `render-engine` → `render_<workbookId>` に統一。旧 `workbook_<id>.lock` は廃止 |
| フィールド所有権 | `Render-Workbook` から `currentExcel*` の書き込みを削除。コミット時のコピー対象からも除外。これらは `Scan-Updates` の専有 |
| レンダリング成功時 | ロック内で最新の `currentExcelHash` と突き合わせ、一致すれば `rendered-unchecked`、不一致なら `excel-updated`（ページは `stale`）。content-pdf は保存する |
| レンダリング失敗時 | `Set-WorkbookRenderError` に `AttemptedSnapshotId` / `AttemptedHash` を追加。試行した版が現在版でなければ `render-error` にせず `excel-updated` のままにし、ページの status も上書きしない。失敗情報は `lastRenderErrorSnapshotId` / `lastRenderErrorHash` に残す。3つの catch 経路すべてに `Get-LastRenderAttemptFor` 経由で試行版を渡す |
| 環境指紋 | `Get-RenderEnvironmentFingerprint` を追加（`excelVersion` / `osVersion` / `activePrinter` / フォント / 印刷プロファイル版。`pcName` は診断情報として主判定から除外）。`Reset-RenderEnvironmentForJob` をレンダリングジョブの開始点2箇所に挿入し、サーバー稼働中1回だけだった取得を毎ジョブに変更 |
| 承認ポリシー | `Get-WorkspacePolicy` / `Test-InputHistoryEnabled` / `Test-SourceRetentionEnabled` を追加。`<dataDir>\common\policy.json` を参照する。`sourceRetentionApproved` は `dataDir` が提出フォルダ配下にあることも確認する |
| 設定マージ | `Merge-ConfigDefaults` を追加し、`Get-AppConfig` がローカル config と `default-config.json` をキー単位で再帰マージするよう変更。既存利用者に新キーが届くようにする |
| 矛盾設定 | `Get-AutoRenderSettings` が `autoRender.enabled=true` かつ履歴未承認のとき自動処理を無効化して警告する |
| スケジューラー | `param` に `-AutoSchedulerPath` / `-ParentProcessId` を追加（Stage 3 で使用） |

### その他

- `app/default-config.json` に `inputHistory` / `autoRender` を追加（`autoRender.enabled` は `false`）
- `docs/POLICY_SAMPLE.json` を追加
- `app/tools/selfcheck.py` に V5 の回帰チェックを追加（フィールド所有権、3つの catch 経路、ハッシュ形式、ID粒度、ロック順序、承認ポリシー、設定マージ、環境の毎ジョブ取得）

## Stage 2 — Phase 1A: 検知版スナップショット

- `input-history\<workbookId>\<snapshotId>\` に manifest / source.xlsx / renders / pins / leases を配置
- `Ensure-SnapshotMetadata`（重複排除）と `Capture-RenderInput`（入力の確保）を分離。同じ内容が再登場して現物が削除済みでも、レンダリング入力を必ず確保する
- 保存手順はハッシュ検証つき（コピー後・移動後の2回）。`manifest.json` を最後に書き、その存在を完成マーカーにする
- コミットは compare-and-set。**PC の時刻ではなく提出ファイルの ticks / size / hash で新旧を判定する**
- pins（`final-pdf_<buildId>` / `comparison-baseline` / `manual`）と leases（期限つき）をファイルの作成・削除で管理
- 掃除は2段階。段階1は `source.xlsx` のみ削除（`sourceRetentionDaysAfterBuild` を適用）、段階2は pin が無いときだけフォルダごと削除
- 常時保護：`currentSnapshotId` / `pendingSnapshotId` / `lastRenderedSnapshotId` / baseline
- `Remove-WorkbookContentPdfs` を保持世代数＋pins/leases 対応に変更。レンダリング中の個別削除は廃止
- 登録解除では履歴を消さない（`Remove-WorkbookContentPdfsAll` は履歴無効時のみ）
- 縮退モード（現物保存が未承認）：`state\jobs\<captureId>\` に一時コピーし、ジョブ完了時と `ephemeralCopyMaxAgeMinutes` 超過で必ず削除

## Stage 3 — Phase 2A: 検知パイプライン

- `server.ps1 -AutoSchedulerPath -ParentProcessId` でスケジューラーモードを起動（独立スクリプトにしない。既存関数を共用するため）
- **親PID監視**で子プロセスを終了。tick は10秒でスリープせず、ブックごとに直列180秒待たない
- ブック単位の所有権 `locks\auto-owner_<workbookId>.lock` を `Try-AcquireLockHandle` で tick をまたいで保持
- 対話型Excelの判定は `MainWindowHandle != 0` と `~$<ファイル名>.xlsx` の存在（単純なプロセス有無では止まりすぎる）
- レンダリングは既存の子ジョブ方式（`Start-RenderJob`）に委譲
- 起動時リカバリー：`rendering` / `ready` を `waiting` へ戻し、消えた `pendingSnapshotId` を破棄

## Stage 4 — Phase 2B: 画像ハッシュと比較

- `PdfPageAnalyzer.java` を追加。**RGB / 150dpi**、ピクセル入力形式を `[幅4][高さ4][RGB行優先]`（alpha 除外）に固定。PNG は保存しない
- 環境指紋は `excelVersion` / `osVersion` / `activePrinter` / フォント / 印刷プロファイルから算出（`pcName` は診断情報で主判定に含めない）
- 比較は3経路：環境一致なら保存済みハッシュ、不一致なら前回 `source.xlsx` を現在環境で再レンダリング、現物が無ければ打ち切って今回版を基準にする
- `Render-SnapshotForComparison` は structure / content-pdf / volumes / 出力履歴を一切変更しない。PDF だけ捨て、`visual-hashes.json` は残す
- baseline は可変ポインタ（`input-history\<workbookId>\comparison-baseline.json`）＋pin。**解析に成功したときだけ**更新する
- 解析は PDF 作成のクリティカルパスの外。失敗しても PDF 作成は成功扱い（判定は `unknown`）

## Stage 5 — Phase 1B / 2C: アーカイブ・レイアウト履歴・出力トランザクション

- 最終PDFアーカイブ（`final.pdf` / `manifest.json` / `metadata.json` / `sha256.txt`）。一時フォルダで完成させてから `buildId` フォルダへ移動する冪等方式
- `metadata.json` の `renderEnvironments` は fingerprint をキーにした辞書（1つの最終PDFが複数PCの出力を含みうるため）。組版したPCは `composerEnvironment`
- レイアウト投影スナップショットと限定復元。適用するのは `title` / `volume` / `enabled` / `order` / `orderManual` / `numberingMode` / `numberingManual` のみ
- 復元前プレビューと `pre-restore` スナップショット（復元の取り消し用）
- `Invoke-FinalBuildTransaction`：**単体出力もまとめて出力も同じ経路**。8段階のジャーナルを副作用の前に書き、対象は `pageCount > 0` の volume のみ
- 復旧は `oldPdfHash` / `newPdfHash` と実ファイルの照合を正とする。どれとも一致しなければ自動で上書きせず `manual-recovery-required`
- 起動時復旧も `final-build_<category>.lock` を取得する

## UI

- 登録済みExcelに「変更」列（`変更 Nシート` / `変更なし` / `判定不能` / `比較不能`）
- ページ構成に変更バッジと「変更分のみ表示」トグル
- Excel画面に自動処理の状態（静止待ち・保留理由・今すぐ実行）と容量警告
- 最終PDF画面に変更履歴タイムライン
- 「本体・補足をまとめて出力」を `/api/final/build-all` の1回呼び出しへ変更（本体だけ成功する状態を作らない）

## 追加API

```
GET  /api/history/timeline      GET  /api/history/snapshots    GET  /api/history/diff
GET  /api/history/content-pdf   POST /api/history/pin          POST /api/history/unpin
GET  /api/layout/snapshots      POST /api/layout/restore/preview  POST /api/layout/restore
GET  /api/final/archives        POST /api/final/build-all
GET  /api/auto/state            POST /api/auto/run-now
```

## 承認と機能ゲート

`<dataDir>\common\policy.json` が未作成、または `inputHistoryApproved` が `false` の間は、
履歴・差分・自動処理はすべて動作せず、V4.1 と同じ挙動になります（サンプル: `docs/POLICY_SAMPLE.json`）。

```
inputHistoryApproved    : 検知版の記録と差分機能
sourceRetentionApproved : 提出Excel現物の保存（false なら一時コピーのみ）
```

`sourceRetentionApproved` は、`dataDir` が提出フォルダ配下にあることも起動時に確認します。
**なお policy.json は技術的なアクセス制御ではなく運用フラグです。**共有 `dataDir` に書き込める利用者は編集できます。

## 検証状況

- `app/tools/selfcheck.py`：**通過**（`PdfPageAnalyzer.class` 未同梱の警告あり）
- 括弧バランス：ベースラインとの差分 0
- **PowerShell の実行・構文チェックは未実施**（Linux 検証環境に `pwsh` が無いため）。Windows での起動確認が必要
- Excel COM / PDF レンダリング / VBS 起動を含むエンドツーエンド試験は未実施
