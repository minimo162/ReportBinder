## V4.5 未登録Excelの幅・更新日時修正（2026-07-28）

- 未登録Excel側の幅を広げ、長いファイル名がより多く表示されるようにしました。
- 未登録Excelの更新日時は、Windowsのファイルハンドルから取得した最終書込時刻をサーバー側でローカル表示文字列へ確定し、Explorerと同じ「年月日 時:分」を表示します。
- ブラウザ側でUTCから再変換する処理をやめ、タイムゾーン変換による表示差を防止しました。
- SMB共有上でディレクトリ情報が古い場合に備え、FILE_READ_ATTRIBUTESでファイルを直接開いて更新日時を取得します。
- 登録済みExcelの「最新PDF」はExcel更新日時ではないことが分かるよう、「PDF作成日時」へ名称を明確化しました。
- CSS/JavaScriptのキャッシュ識別子をV4.5へ更新しました。

## V4.4 Excel一覧の日時・列幅修正（2026-07-28）

- 未登録Excelの更新日時は秒を表示せず、年月日・時・分のみ表示するようにしました。
- 共有フォルダではFileInfoの列挙キャッシュを避けるため、ファイルパスからUTC更新日時を直接再取得し、ブラウザ側のローカル時刻へ変換して表示します。
- 登録済みExcelの「シート数」「ページ数」列を削除しました。
- 登録済みExcel表を「ファイル名／最新PDF／状態」の3情報に整理し、横スクロールなしでカード内に収まるよう列幅を調整しました。
- CSS/JavaScriptのキャッシュ識別子をV4.4へ更新しました。

## V4.3 Excel登録画面の可読性・操作性改善（2026-07-28）

- Excel登録画面の左右比率を50:50から未登録約36%・登録済み約64%へ変更し、登録済みテーブルの全列を確認しやすくしました。
- 未登録Excelの「シート数」列を削除しました。登録前にExcel COMを開かないため、不要な空欄表示もなくしました。
- 「選択分をPDF作成」ボタンを登録済みカード上部へ移し、「必要分を選択」の直後に実行できるようにしました。
- BIZ UDPゴシックを最優先フォントにし、本文・ボタン・表・キャプションのフォントサイズを拡大しました。
- 未登録Excelの更新日時を秒まで表示し、一覧取得時刻を併記しました。共有フォルダのFileInfoを毎回再取得・Refreshしてから日時を返します。
- CSS/JavaScriptのキャッシュ識別子をV4.3へ更新しました。

## V4.2 (2026-07-28)

- PDF作成結果の反映処理で、PowerShellの読み取り専用自動変数 `$PID` と衝突する `$pid` を使用していた問題を修正しました。
- ページID用の変数名を `$pageId` に変更しました。
- `selfcheck.py` に `$pid = ...` の再混入を検出する静的チェックを追加しました。

# ReportBinder V4 変更履歴

## V4.1 初回提出フォルダ選択の修正（2026-07-28）

- 初回の提出フォルダ選択時、ローカル設定の保存前に管理データ初期化が `Get-Paths` を読み直し、`管理データフォルダが未設定です` で失敗する問題を修正しました。
- `Ensure-Package` から選択済みの `dataDir` を初期化・schema移行処理へ明示的に渡し、初回設定でも `ja/en` のstructureを作成できるようにしました。
- 現行画面では表示専用となった管理データ・出力先要素を、旧互換関数が入力欄として扱わないよう修正しました。
- selfcheckに初回workspace初期化経路の回帰チェックを追加しました。

## V4.0 UI/UX・状態管理改修

## サーバー／データ構造
- structure.json を言語別6キー（main/appendix × ECM/BOD/DMM）へ移行
- schemaVersion 1からの移行をstructureロック内で実行し、移行前バックアップを保存
- structure更新を読込・変更・保存の単位でトランザクション化
- 最終PDF系APIのcategoryを必須化し、欠落・不正値はHTTP 400で拒否
- 最終PDFのreadinessとfingerprint判定を共通化
- builtFingerprintによる鮮度判定、staleReasonsによる理由表示を追加
- manifest、一時PDF、volumeロックをカテゴリ別の決定的名称へ変更
- 組版中に入力が変わった場合の競合検知を追加
- ページorderを10刻みに統一し、シート名順への復元APIを追加

## UI/UX
- ECM/BOD/DMMのグローバル切替と4ステップバーを常設
- 最終PDFの最新／再出力必要／未出力／出力不能／ファイル消失を表示
- サイドメニューに要対応件数・状態を表示
- PDF作成後・ページ構成後・最終出力後の次アクション導線を追加
- 入力・ドラッグ・モーダル操作中のポーリング再描画を抑止
- シート名順への並べ替え、キーボード移動、追加ページのハイライトを追加
- 到達不能な機能を接続し、死んでいるページャ・行メニュー・情報記号等を削除
- Apple系の余白とLinear系の1px UIへ刷新

## 配布・運用
- 共有default-config.jsonとローカルconfig.jsonへ分離
- ログ、実運用設定、一時ジョブ、キャッシュを配布物から除外
- オンライン導入版とJRE同梱のオフライン完結版を用意
- README、API仕様、第三者ライブラリ導入手順、ライセンス通知を更新
